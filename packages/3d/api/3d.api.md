## API Report File for "@ignifx/3d"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { AnimationGroup } from '@babylonjs/lite';
import { AnimationManager } from '@babylonjs/lite';
import { App } from '@ignifx/core';
import { AssetHandle } from '@ignifx/core';
import { AssetLoader } from '@ignifx/core';
import { Camera } from '@ignifx/core';
import { CharacterController } from '@ignifx/physics';
import { Component } from '@ignifx/core';
import { ComponentHooks } from '@ignifx/core';
import { Entity } from '@ignifx/core';
import { ErrorContext } from '@ignifx/core';
import { Extension } from '@ignifx/core';
import { IgnifxError } from '@ignifx/core';
import { MeshRenderer } from '@ignifx/core';
import { MutableVec3 } from '@ignifx/core';
import { NavCrowd } from '@babylonjs/lite';
import { NavigationPlugin } from '@babylonjs/lite';
import { ObstacleHandle } from '@babylonjs/lite';
import { Rigidbody } from '@ignifx/physics';
import { Schema } from '@ignifx/core';
import { SchemaDescription } from '@ignifx/core';
import { Script } from '@ignifx/core';
import { Signal } from '@ignifx/core';
import { System } from '@ignifx/core';
import { SystemContext } from '@ignifx/core';
import { Vec3 } from '@ignifx/core';
import { Vec3Like } from '@ignifx/core';
import { World } from '@ignifx/core';

// @public
export class Animator extends Component implements ComponentHooks {
    constructor();
    // @internal
    advance(deltaSeconds: number): void;
    static allowMultiple: boolean;
    animator: AssetHandle<AnimatorAsset> | null;
    applyOnAwake: boolean;
    crossFade(state: string, seconds: number, layer?: string): void;
    currentState(layer?: string): string;
    defaultLayer: string;
    getBool(name: string): boolean;
    getFloat(name: string): number;
    get isReady(): boolean;
    get lite(): {
        readonly manager: LiteAnimationManager | null;
    };
    normalizedTime(layer?: string): number;
    onAttach(): void;
    onDetach(): void;
    get onEvent(): Signal<string>;
    get onStateEntered(): Signal<string>;
    get onStateExited(): Signal<string>;
    play(state: string, options?: AnimatorPlayOptions): void;
    static schema: Schema;
    setBool(name: string, value: boolean): void;
    setFloat(name: string, value: number): void;
    setInt(name: string, value: number): void;
    setTrigger(name: string): void;
    speed: number;
    get stateMachine(): AnimatorStateMachine | null;
    static typeId: string;
    updateWhenPaused: boolean;
}

// @public
export const ANIMATOR_ASSET_TYPE = "animator";

// @public
export const ANIMATOR_CONDITION_OPS: readonly ["gt", "gte", "lt", "lte", "eq", "neq", "trigger"];

// @public
export const ANIMATOR_FILE_EXTENSIONS: readonly string[];

// @public
export const ANIMATOR_FORMAT = "ignifx.animator";

// @public
export const ANIMATOR_FORMAT_VERSION = 1;

// @public
export const ANIMATOR_MASK_MODES: readonly ["include", "exclude"];

// @public
export const ANIMATOR_PARAMETER_KINDS: readonly ["float", "int", "bool", "trigger"];

// @public
export class AnimatorAsset {
    constructor(address: string, definition: AnimatorDefinition);
    readonly address: string;
    static assetType: string;
    clipNames(): readonly string[];
    readonly definition: AnimatorDefinition;
    layer(name: string): AnimatorLayerDefinition | null;
    get layerNames(): readonly string[];
    state(name: string): AnimatorStateDefinition | null;
    get stateNames(): readonly string[];
}

// @public
export interface AnimatorBlendChildDefinition {
    readonly clip: string;
    readonly threshold: number;
}

// @public
export interface AnimatorBlendTreeDefinition {
    readonly children: readonly AnimatorBlendChildDefinition[];
    readonly name: string;
    readonly param: string;
}

// @public
export interface AnimatorConditionDefinition {
    readonly op: AnimatorConditionOp;
    readonly param: string;
    readonly value: number;
}

// @public
export type AnimatorConditionOp = (typeof ANIMATOR_CONDITION_OPS)[number];

// @public
export interface AnimatorDefinition {
    readonly blendTrees1D: readonly AnimatorBlendTreeDefinition[];
    readonly format: typeof ANIMATOR_FORMAT;
    readonly formatVersion: number;
    readonly layers: readonly AnimatorLayerDefinition[];
    readonly parameters: readonly AnimatorParameterDefinition[];
    readonly states: readonly AnimatorStateDefinition[];
    readonly transitions: readonly AnimatorTransitionDefinition[];
}

// @public
export interface AnimatorEventDefinition {
    readonly name: string;
    readonly time: number;
}

// @public
export function animatorFileSchema(): Schema;

// @public
export interface AnimatorInput {
    readonly blendTrees1D?: readonly unknown[];
    readonly format?: string;
    readonly formatVersion?: number;
    readonly layers?: readonly unknown[];
    readonly parameters?: readonly unknown[];
    readonly states?: readonly unknown[];
    readonly transitions?: readonly unknown[];
}

// @public
export interface AnimatorLayerDefinition {
    readonly additive: boolean;
    readonly defaultState: string;
    readonly mask: readonly string[];
    readonly maskMode: AnimatorMaskMode;
    readonly name: string;
    readonly weight: number;
}

// @public
export type AnimatorMaskMode = (typeof ANIMATOR_MASK_MODES)[number];

// @public
export interface AnimatorParameterDefinition {
    readonly kind: AnimatorParameterKind;
    readonly name: string;
    readonly value: number;
}

// @public
export type AnimatorParameterKind = (typeof ANIMATOR_PARAMETER_KINDS)[number];

// @public
export interface AnimatorPlayOptions {
    readonly layer?: string;
    readonly transitionSeconds?: number;
}

// @public
export interface AnimatorStateDefinition {
    readonly blendTree: string;
    readonly clip: string;
    readonly events: readonly AnimatorEventDefinition[];
    readonly layer: string;
    readonly loop: boolean;
    readonly name: string;
    readonly speed: number;
}

// @public
export class AnimatorStateMachine {
    constructor(definition: AnimatorDefinition);
    advance(deltaSeconds: number): void;
    get clips(): readonly ClipWeight[];
    crossFade(state: string, seconds: number, layer?: string): void;
    currentState(layer?: string): string;
    get definition(): AnimatorDefinition;
    drainEvents(out: string[]): void;
    drainStateChanges(out: StateChange[]): void;
    getBool(name: string): boolean;
    getFloat(name: string): number;
    isInTransition(layer?: string): boolean;
    isTriggerSet(name: string): boolean;
    normalizedTime(layer?: string): number;
    play(state: string, options?: PlayStateOptions): void;
    resetTrigger(name: string): void;
    setBool(name: string, value: boolean): void;
    setClipLength(clip: string, seconds: number): void;
    setFloat(name: string, value: number): void;
    setInt(name: string, value: number): void;
    setTrigger(name: string): void;
    speed: number;
}

// @public
export interface AnimatorTransitionDefinition {
    readonly conditions: readonly AnimatorConditionDefinition[];
    readonly duration: number;
    readonly exitTime: number | null;
    readonly from: string;
    readonly interruptible: boolean;
    readonly to: string;
}

// @public
export const ANY_STATE = "any";

// @public
export class Billboard extends Component {
    constructor();
    static allowMultiple: boolean;
    // @internal
    face(eye: Vec3Like, cameraForward: Vec3Like): void;
    faceCameraPlane: boolean;
    mode: BillboardMode;
    static schema: Schema;
    static typeId: string;
}

// @public
export const BILLBOARD_MODES: readonly ["full", "yAxis"];

// @public
export const BILLBOARD_ORDER = 20;

// @public
export type BillboardMode = (typeof BILLBOARD_MODES)[number];

// @public
export class BillboardSystem implements System {
    readonly name = "ignifx/3d-billboard";
    update(ctx: SystemContext): void;
}

// @public
export function cameraRelativeToRef<TOut extends MutableVec3>(inputX: number, inputY: number, cameraForward: Vec3Like, out: TOut): TOut;

// @public
export interface ClipWeight {
    readonly additive: boolean;
    readonly clip: string;
    readonly layer: string;
    readonly speed: number;
    readonly weight: number;
}

// @public
export function createAnimatorLoader(): AssetLoader<AnimatorAsset>;

// @public
export const DEFAULT_CLIP_LENGTH = 1;

// @public
export function defaultStateOf(definition: AnimatorDefinition, layer: AnimatorLayerDefinition): string;

// @public
export function defaultThreeDSettings(): ThreeDSettings;

// @public
export function defineAnimator(input: AnimatorInput, address?: string): AnimatorDefinition;

// @public
export function describeAnimatorFormat(): SchemaDescription;

// @public
export function describeSchemas(): Readonly<Record<string, SchemaDescription>>;

// @public
export class FirstPersonController extends Script {
    constructor();
    airControl: number;
    static allowMultiple: boolean;
    awake(): void;
    cameraPivot: Entity | null;
    coyoteTime: number;
    crouchAction: string;
    crouchHeight: number;
    crouchSpeed: number;
    fixedUpdate(dt: number): void;
    gravity: number;
    headBobAmplitude: number;
    headBobFrequency: number;
    invertY: boolean;
    get isCrouched(): boolean;
    get isGrounded(): boolean;
    get isSprinting(): boolean;
    jumpAction: string;
    jumpBufferTime: number;
    jumpHeight: number;
    lockPointerOnClick: boolean;
    lookAction: string;
    moveAction: string;
    onDisable(): void;
    get pitch(): number;
    rebind(): void;
    static requires: readonly [typeof CharacterController];
    static schema: Schema;
    sensitivity: number;
    get speed(): number;
    sprintAction: string;
    sprintFovKick: number;
    sprintSpeed: number;
    standHeight: number;
    static typeId: string;
    update(dt: number): void;
    get verticalVelocity(): number;
    walkSpeed: number;
    get yaw(): number;
}

// @public
export class JumpTimers {
    get bufferRemaining(): number;
    consume(): boolean;
    get coyoteRemaining(): number;
    reset(): void;
    step(deltaSeconds: number, isGrounded: boolean, jumpPressed: boolean, coyoteSeconds: number, bufferSeconds: number): void;
}

// @public
export function jumpVelocity(height: number, gravity: number): number;

// @beta
export type LiteAnimationGroup = AnimationGroup;

// @beta
export type LiteAnimationManager = AnimationManager;

// @beta
export type LiteNavCrowd = NavCrowd;

// @beta
export type LiteNavigationPlugin = NavigationPlugin;

// @beta
export type LiteObstacleHandle = ObstacleHandle;

// @public
export const LOD_CULLED = -1;

// @public
export const LOD_ORDER = -10;

// @public
export class LodGroup extends Component implements ComponentHooks {
    constructor();
    static allowMultiple: boolean;
    // @internal
    evaluate(distance: number): void;
    hysteresis: number;
    get level(): number;
    levels: LodLevel[];
    onDetach(): void;
    get onLevelChanged(): Signal<number>;
    static schema: Schema;
    static typeId: string;
}

// @public
export interface LodLevel {
    readonly distance: number;
    readonly renderer: MeshRenderer | null;
}

// @public
export class LodSystem implements System {
    readonly name = "ignifx/3d-lod";
    update(ctx: SystemContext): void;
}

// @public
export function mainCamera(world: World): Camera | null;

// @public
export function mainCameraForward(world: World): Vec3Like;

// @public
export const NAV_OBSTACLE_SHAPES: readonly ["box", "cylinder"];

// @public
export const NAVIGATION_ORDER = 200;

// @public
export class NavigationService {
    // @internal
    constructor(app: App, locateFile: ((url: string) => string) | null);
    // @internal
    acquirePlugin(): Promise<LiteNavigationPlugin>;
    closestPoint(point: Vec3Like, out?: MutableVec3): MutableVec3 | null;
    dispose(): void;
    findPath(from: Vec3Like, to: Vec3Like): readonly Vec3[];
    get isLoaded(): boolean;
    get onReady(): Signal<NavigationService>;
    get primarySurface(): NavMeshSurface | null;
    raycast(from: Vec3Like, to: Vec3Like, out?: MutableVec3): MutableVec3 | null;
    get surfaces(): readonly NavMeshSurface[];
}

// @public
export class NavigationSystem implements System {
    constructor(service: NavigationService);
    readonly name = "ignifx/3d-navigation";
    update(ctx: SystemContext): void;
}

// @public
export class NavMeshAgent extends Component implements ComponentHooks {
    constructor();
    acceleration: number;
    get agentIndex(): number;
    static allowMultiple: boolean;
    get destination(): Vec3Like;
    // @internal
    ensureJoined(fallback: NavMeshSurface | null): boolean;
    height: number;
    get isOnNavMesh(): boolean;
    get isStopped(): boolean;
    get onArrived(): Signal<NavMeshAgent>;
    onDetach(): void;
    radius: number;
    get remainingDistance(): number;
    static schema: Schema;
    separationWeight: number;
    setDestination(point: Vec3Like): boolean;
    speed: number;
    stop(): void;
    stoppingDistance: number;
    surface: Entity | null;
    // @internal
    syncFromCrowd(): void;
    static typeId: string;
    updatePosition: boolean;
    updateRotation: boolean;
    get velocity(): Vec3Like;
}

// @public
export class NavMeshObstacle extends Component implements ComponentHooks {
    constructor();
    static allowMultiple: boolean;
    // @internal
    carve(fallback: NavMeshSurface | null): boolean;
    height: number;
    get isCarved(): boolean;
    onDetach(): void;
    radius: number;
    remove(): void;
    static schema: Schema;
    shape: NavObstacleShape;
    size: Vec3Like;
    surface: Entity | null;
    static typeId: string;
}

// @public
export class NavMeshSurface extends Component implements ComponentHooks {
    constructor();
    addSource(positions: ArrayLike<number>, indices: ArrayLike<number>, worldMatrix: ArrayLike<number> | null): void;
    agentClimb: number;
    agentHeight: number;
    agentRadius: number;
    static allowMultiple: boolean;
    bake(): Promise<boolean>;
    bakeOnAwake: boolean;
    cellHeight: number;
    cellSize: number;
    clearSources(): void;
    closestPoint(point: Vec3Like, out?: MutableVec3): MutableVec3 | null;
    // Warning: (ae-incompatible-release-tags) The symbol "crowd" is marked as @public, but its signature references "LiteNavCrowd" which is marked as @beta
    get crowd(): LiteNavCrowd | null;
    detailSampleDistance: number;
    detailSampleMaxError: number;
    findPath(from: Vec3Like, to: Vec3Like): readonly Vec3[];
    get isBaked(): boolean;
    get isBaking(): boolean;
    layers: readonly string[];
    get lite(): {
        readonly plugin: LiteNavigationPlugin | null;
        readonly crowd: LiteNavCrowd | null;
    };
    maxAgentRadius: number;
    maxAgents: number;
    maxEdgeLength: number;
    maxObstacles: number;
    maxSimplificationError: number;
    maxVertsPerPoly: number;
    mergeRegionArea: number;
    minRegionArea: number;
    get onBaked(): Signal<NavMeshSurface>;
    onDetach(): void;
    // Warning: (ae-incompatible-release-tags) The symbol "plugin" is marked as @public, but its signature references "LiteNavigationPlugin" which is marked as @beta
    get plugin(): LiteNavigationPlugin | null;
    prebaked: string;
    randomSeed: number;
    raycast(from: Vec3Like, to: Vec3Like, out?: MutableVec3): MutableVec3 | null;
    // @internal
    reserveAgent(): boolean;
    static schema: Schema;
    get scratch(): MutableVec3;
    get sourceCount(): number;
    tileSize: number;
    static typeId: string;
    walkableSlopeAngle: number;
}

// @public
export type NavObstacleShape = (typeof NAV_OBSTACLE_SHAPES)[number];

// @public
export class PlatformMover extends Script {
    constructor();
    static allowMultiple: boolean;
    awake(): void;
    carryRiders: boolean;
    get deltaThisStep(): Vec3Like;
    duration: number;
    fixedUpdate(dt: number): void;
    offset: Vec3Like;
    static requires: readonly [typeof Rigidbody];
    get riderCount(): number;
    static schema: Schema;
    static typeId: string;
    waitSeconds: number;
}

// @public
export interface PlayStateOptions {
    readonly layer?: string;
    readonly transitionSeconds?: number;
}

// @public
export class Projectile extends Script {
    constructor();
    get age(): number;
    static allowMultiple: boolean;
    awake(): void;
    destroyOnHit: boolean;
    fixedUpdate(dt: number): void;
    gravityScale: number;
    lifetimeSeconds: number;
    onCollisionEnter(collision: unknown): void;
    owner: Entity | null;
    static requires: readonly [typeof Rigidbody];
    static schema: Schema;
    speed: number;
    static typeId: string;
}

// @public
export function projectOnSlopeToRef<TOut extends MutableVec3>(direction: Vec3Like, normal: Vec3Like, out: TOut): TOut;

// @public
export class RigidbodyMover extends Script {
    constructor();
    static allowMultiple: boolean;
    awake(): void;
    cameraRelative: boolean;
    fixedUpdate(dt: number): void;
    force: number;
    maxSpeed: number;
    moveAction: string;
    static requires: readonly [typeof Rigidbody];
    static schema: Schema;
    torqueSteering: boolean;
    static typeId: string;
}

// @public
export function slopeAngleDegrees(normal: Vec3Like): number;

// @public
export interface StateChange {
    readonly entered: boolean;
    readonly layer: string;
    readonly state: string;
}

// @public
export class ThirdPersonCamera extends Script {
    constructor();
    static allowMultiple: boolean;
    awake(): void;
    collisionEnabled: boolean;
    collisionLayers: readonly string[];
    collisionRadius: number;
    collisionRecoverySpeed: number;
    get currentDistance(): number;
    damping: number;
    distance: number;
    invertY: boolean;
    lateUpdate(dt: number): void;
    lookAction: string;
    maxPitch: number;
    minPitch: number;
    get pitch(): number;
    get pivot(): Vec3Like;
    rebind(): void;
    static schema: Schema;
    sensitivity: number;
    shoulderOffset: Vec3Like;
    snap(): void;
    target: Entity | null;
    static typeId: string;
    get yaw(): number;
}

// @public
export class ThirdPersonController extends Script {
    constructor();
    airControl: number;
    static allowMultiple: boolean;
    awake(): void;
    coyoteTime: number;
    fixedUpdate(dt: number): void;
    gravity: number;
    get isGrounded(): boolean;
    get isSprinting(): boolean;
    jumpAction: string;
    jumpBufferTime: number;
    jumpHeight: number;
    moveAction: string;
    get moveDirection(): Vec3Like;
    rebind(): void;
    static requires: readonly [typeof CharacterController];
    resetMomentum(): void;
    rotateToMovement: boolean;
    static schema: Schema;
    slideSpeed: number;
    get speed(): number;
    sprintAction: string;
    sprintSpeed: number;
    stepHeight: number;
    turnSpeed: number;
    static typeId: string;
    get verticalVelocity(): number;
    walkSpeed: number;
}

// @public
export const THREE_D_ANIMATION_ORDER = 10;

// @public
export const THREE_D_ERROR_MESSAGES: Readonly<Record<string, string>>;

// @public
export const THREE_D_SETTINGS_SECTION = "threeD";

// @public
export const threeD: (options?: ThreeDOptions) => Extension;

// @public
export class ThreeDAnimationSystem implements System {
    readonly name = "ignifx/3d-animation";
    update(ctx: SystemContext): void;
}

// @public
export function threeDError(code: ThreeDErrorCode, message: string, options?: ThreeDErrorOptions): IgnifxError;

// @public
export const ThreeDErrorCode: {
    readonly invalidAnimatorFile: "IGX-1201";
    readonly unknownState: "IGX-1202";
    readonly unknownParameter: "IGX-1203";
    readonly parameterKindMismatch: "IGX-1204";
    readonly navigationNotReady: "IGX-1205";
    readonly navigationUnavailable: "IGX-1206";
    readonly crowdFull: "IGX-1207";
    readonly obstaclesNotEnabled: "IGX-1208";
    readonly emptyNavMesh: "IGX-1209";
    readonly prebakedNavMeshUnsupported: "IGX-1210";
    readonly noMainCamera: "IGX-1211";
    readonly unknownInputAction: "IGX-1212";
    readonly duplicateExtension: "IGX-1213";
    readonly invalidLodLevel: "IGX-1214";
};

// @public
export type ThreeDErrorCode = (typeof ThreeDErrorCode)[keyof typeof ThreeDErrorCode];

// @public
export interface ThreeDErrorOptions {
    readonly cause?: unknown;
    readonly context?: ErrorContext;
    readonly hint?: string;
}

// @public
export interface ThreeDOptions {
    readonly autoBakeNavMesh?: boolean;
    readonly navigationSeed?: number;
    readonly navigationWasmUrl?: string;
}

// @public
export interface ThreeDSettings {
    readonly autoBakeNavMesh: boolean;
    readonly navigationSeed: number;
    readonly navigationWasmUrl: string;
}

// @public
export function threeDSettingsSchema(): Schema;

// @public
export function turnTowardsDegrees(currentDegrees: number, targetDegrees: number, degreesPerSecond: number, deltaSeconds: number): number;

// @public
export const VERSION = "0.0.0";

// @public
export const WORLD_FORWARD: Vec3Like;

// @public
export function yawFromDirection(x: number, z: number): number | null;

// Warnings were encountered during analysis:
//
// src/animator/animator.ts:159:5 - (ae-incompatible-release-tags) The symbol "manager" is marked as @public, but its signature references "LiteAnimationManager" which is marked as @beta
// src/navigation/nav-mesh-surface.ts:250:5 - (ae-incompatible-release-tags) The symbol "plugin" is marked as @public, but its signature references "LiteNavigationPlugin" which is marked as @beta
// src/navigation/nav-mesh-surface.ts:250:17 - (ae-incompatible-release-tags) The symbol "crowd" is marked as @public, but its signature references "LiteNavCrowd" which is marked as @beta

// (No @packageDocumentation comment for this package)

```
