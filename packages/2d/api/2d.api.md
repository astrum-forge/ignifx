## API Report File for "@ignifx/2d"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { App } from '@ignifx/core';
import { AssetHandle } from '@ignifx/core';
import { AssetLoader } from '@ignifx/core';
import { Bounds2D } from '@babylonjs/lite';
import { ColorLike } from '@ignifx/core';
import { Component } from '@ignifx/core';
import { ComponentHooks } from '@ignifx/core';
import { Entity } from '@ignifx/core';
import { ErrorContext } from '@ignifx/core';
import { Extension } from '@ignifx/core';
import { IgnifxError } from '@ignifx/core';
import { JsonSchemaObject } from '@ignifx/core';
import { MutableVec2 } from '@ignifx/core';
import { SceneInstance } from '@ignifx/core';
import { Schema } from '@ignifx/core';
import { SchemaDescription } from '@ignifx/core';
import { Script } from '@ignifx/core';
import { Signal } from '@ignifx/core';
import { Sprite2DCustomShader } from '@babylonjs/lite';
import { Sprite2DHandle } from '@babylonjs/lite';
import { Sprite2DLayer } from '@babylonjs/lite';
import { Sprite2DProps } from '@babylonjs/lite';
import { Sprite2DView } from '@babylonjs/lite';
import { SpriteAtlas } from '@babylonjs/lite';
import { SpriteBlendMode } from '@babylonjs/lite';
import { SpriteFrame } from '@babylonjs/lite';
import { SpritePickInfo } from '@babylonjs/lite';
import { SpriteRenderer as SpriteRenderer_2 } from '@babylonjs/lite';
import { SpriteSampling } from '@babylonjs/lite';
import { System } from '@ignifx/core';
import { SystemContext } from '@ignifx/core';
import { Texture2D } from '@babylonjs/lite';
import { Vec2Like } from '@ignifx/core';
import { Vec4Like } from '@ignifx/core';
import { World } from '@ignifx/core';

// @public
export interface AnimatedTilemapSink {
    advanceAnimatedTiles(world: World, deltaSeconds: number): void;
}

// @public
export interface AsepriteAnimationImportOptions {
    readonly atlas?: string;
    readonly defaultFps?: number;
    readonly frameNameOf?: (index: number) => string;
}

// @public
export function asepriteFrameName(raw: string): string;

// @public
export interface AsepriteImportOptions {
    readonly image?: string;
    readonly premultipliedAlpha?: boolean;
    readonly sampling?: "linear" | "nearest";
}

// @public
export class Camera2D extends Component {
    constructor();
    static allowMultiple: boolean;
    boundsMax: Vec2Like | null;
    boundsMin: Vec2Like | null;
    get centre(): Vec2Like;
    deadZone: Vec2Like;
    follow: Entity | null;
    followDamping: number;
    followOffset: Vec2Like;
    orthographicSize: number;
    pixelPerfect: boolean;
    priority: number;
    referenceResolution: Vec2Like;
    // @internal
    resolve(worldX: number, worldY: number, viewportWidthPx: number, viewportHeightPx: number, pixelsPerUnit: number): void;
    static schema: Schema;
    screenToWorld(x: number, y: number, out?: MutableVec2): MutableVec2;
    static typeId: string;
    get viewportSizePx(): Vec2Like;
    worldToScreen(point: Vec2Like, out?: MutableVec2): MutableVec2;
    get zoom(): number;
}

// @public
export class Camera2DFollow extends Script {
    static allowMultiple: boolean;
    awake(): void;
    lateUpdate(deltaTime: number): void;
    static typeId: string;
}

// @public
export interface CollisionMergeOptions {
    readonly cellSize: number;
    readonly chunkSize: number;
    readonly height: number;
    readonly width: number;
}

// @public
export function createSpriteAnimationLoader(): AssetLoader<SpriteAnimationAsset>;

// @public
export function createSpriteAtlasLoader(): AssetLoader<SpriteAtlasAsset>;

// @public
export function createTilemapLoader(): AssetLoader<TilemapAsset>;

// @public
export function decodeTileRle(rle: readonly number[]): readonly number[];

// @public
export const DEFAULT_CHUNK_SIZE = 32;

// @public
export const DEFAULT_CLIP_FPS = 12;

// @public
export const DEFAULT_ORTHOGRAPHIC_SIZE = 5;

// @public
export const DEFAULT_PIXELS_PER_UNIT = 100;

// @public
export const DEFAULT_REFERENCE_RESOLUTION: Vec2Like;

// @public
export const DEFAULT_SORTING_LAYER = "Default";

// @public
export function defaultTwoDSettings(): TwoDSettings;

// @public
export function defineSpriteAnimation(input: SpriteAnimationInput, address?: string): SpriteAnimationDefinition;

// @public
export function defineSpriteAtlas(input: SpriteAtlasInput, address?: string): SpriteAtlasDefinition;

// @public
export function defineTilemap(input: TilemapInput, address?: string): TilemapDefinition;

// @public
export function describeSchemas(): Readonly<Record<string, SchemaDescription>>;

// @public
export function describeSpriteAnimationFormat(): SchemaDescription;

// @public
export function describeSpriteAtlasFormat(): SchemaDescription;

// @public
export function describeTilemapFormat(): SchemaDescription;

// @public
export function describeTwoDSchemas(): Readonly<Record<string, SchemaDescription>>;

// @public
export const EMPTY_TILE_ID = 0;

// @public
export function encodeTileRle(tiles: readonly number[]): readonly number[];

// @public
export function fieldInstancesToRecord(fields: unknown): Readonly<Record<string, string | number | boolean>>;

// @public
export function findTileset(map: TilemapDefinition, tileId: number): TilesetDefinition | null;

// @public
export function gridAtlas(options: GridAtlasImportOptions): SpriteAtlasDefinition;

// @public
export interface GridAtlasImportOptions {
    readonly cellHeight: number;
    readonly cellWidth: number;
    readonly columns?: number;
    readonly image: string;
    readonly imageHeight: number;
    readonly imageWidth: number;
    readonly margin?: number;
    readonly namePrefix?: string;
    readonly pivot?: Vec2Like;
    readonly premultipliedAlpha?: boolean;
    readonly rows?: number;
    readonly sampling?: "linear" | "nearest";
    readonly spacing?: number;
}

// @public
export function importAsepriteAnimations(json: unknown, options?: AsepriteAnimationImportOptions): SpriteAnimationDefinition;

// @public
export function importAsepriteAtlas(json: unknown, options?: AsepriteImportOptions): SpriteAtlasDefinition;

// @public
export function importLdtkLevel(ldtk: unknown, options?: LdtkImportOptions): TilemapDefinition;

// @public
export function importTexturePackerAtlas(json: unknown, options?: TexturePackerImportOptions): SpriteAtlasDefinition;

// @public
export function importTiledMap(tmj: unknown, options?: TiledImportOptions): TilemapDefinition;

// @public
export function isFullCellSolid(info: TileCollisionInfo, cellSize: number): boolean;

// @public
export const LDTK_DEFAULT_INTGRID_COLLIDERS: Readonly<Record<number, TileColliderDefinition>>;

// @public
export const LDTK_INTGRID_TILESET_NAME = "intgrid";

// @public
export interface LdtkImportOptions {
    readonly atlasFor?: (relPath: string) => string;
    readonly intGridColliders?: Readonly<Record<number, TileColliderDefinition>>;
    readonly level?: string;
    readonly pixelsPerUnit?: number;
    readonly sortingLayer?: string;
}

// @public
export type LiteAtlasTexture = Texture2D;

// @public
export type LiteBounds2D = Bounds2D;

// @public
export type LiteSprite2DHandle = Sprite2DHandle;

// @public
export type LiteSprite2DLayer = Sprite2DLayer;

// @public
export type LiteSprite2DView = Sprite2DView;

// @public
export type LiteSpriteAtlas = SpriteAtlas;

// @public
export type LiteSpriteBlendMode = SpriteBlendMode;

// @public
export type LiteSpriteFrame = SpriteFrame;

// @public
export type LiteSpritePickInfo = SpritePickInfo;

// @public
export type LiteSpriteRenderer = SpriteRenderer_2;

// @public
export type LiteSpriteSampling = SpriteSampling;

// @public
export function mergeTileCollisions(infoAt: (x: number, y: number) => TileCollisionInfo, options: CollisionMergeOptions, version: number): TilemapCollisionData;

// @public
export function normalisePath(path: string): string;

// @public
export class ParallaxLayer extends Component {
    constructor();
    static allowMultiple: boolean;
    // @internal
    applyTo(entries: readonly SpriteLayerEntry[], camera: Camera2D, pixelsPerUnit: number): void;
    factor: Vec2Like;
    repeatHeight: number;
    repeatWidth: number;
    repeatX: boolean;
    repeatY: boolean;
    static schema: Schema;
    sortingLayer: string;
    static typeId: string;
}

// @public
export function parseSpriteFragment(fragment: string | null): string | null;

// @public
export function pivotedPositionToRef<TOut extends MutableVec2>(anchorXPx: number, anchorYPx: number, pivot: Vec2Like, widthPx: number, heightPx: number, rotationRadians: number, out: TOut): TOut;

// @public
export function pixelsToWorldToRef<TOut extends MutableVec2>(xPx: number, yPx: number, pixelsPerUnit: number, out: TOut): TOut;

// @public
export interface PlayClipOptions {
    readonly restart?: boolean;
}

// @public
export function readVec2(value: Vec2Json | undefined, fallback: Vec2Like): Vec2Like;

// @public
export function resolveClipFrames(clip: SpriteClipDefinition, indexOf: (name: string) => number): readonly number[];

// @public
export function resolveRelative(base: string, reference: string): string;

// @public
export function selectCamera(world: World): Camera2D | null;

// @public
export function sizeForZoom(viewportHeightPx: number, zoom: number, pixelsPerUnit: number): number;

// @public
export function snapPixel(valuePx: number, zoom: number): number;

// @public
export function snapZoomToInteger(zoom: number): number;

// @public
export const SORTING_LAYER_ORDER_STEP = 1e3;

// @public
export class SortingLayerTable {
    constructor(names: readonly string[]);
    indexOf(name: string): number;
    get names(): readonly string[];
    orderOf(name: string): number;
    require(name: string): number;
}

// @public
export function spawnTilemapObjects(app: App, service: TwoDService, tilemap: Tilemap): readonly Entity[];

// @public
export const SPRITE_ANIMATION_ASSET_TYPE = "spriteanimation";

// @public
export const SPRITE_ANIMATION_FILE_EXTENSIONS: readonly string[];

// @public
export const SPRITE_ANIMATION_FORMAT = "ignifx.spriteanimation";

// @public
export const SPRITE_ANIMATION_FORMAT_VERSION = 1;

// @public
export const SPRITE_ATLAS_ASSET_TYPE = "spriteatlas";

// @public
export const SPRITE_ATLAS_FILE_EXTENSIONS: readonly string[];

// @public
export const SPRITE_ATLAS_FORMAT = "ignifx.spriteatlas";

// @public
export const SPRITE_ATLAS_FORMAT_VERSION = 1;

// @public
export const SPRITE_BLEND_MODES: readonly ["alpha", "premultiplied", "additive", "multiply", "opaque"];

// @public
export const SPRITE_EFFECT_KINDS: readonly ["tint", "custom"];

// @public
export const SPRITE_FRAME_FRAGMENT_PREFIX = "frame:";

// @public
export class SpriteAnimationAsset {
    // @internal
    constructor(address: string, definition: SpriteAnimationDefinition, atlasAddress: string);
    readonly address: string;
    static assetType: string;
    readonly atlasAddress: string;
    clipNames(): readonly string[];
    get defaultClipName(): string;
    readonly definition: SpriteAnimationDefinition;
    requireClip(name: string, atlas: SpriteAtlasAsset): SpriteClip;
    resolve(atlas: SpriteAtlasAsset): ReadonlyMap<string, SpriteClip>;
}

// @public
export interface SpriteAnimationDefinition {
    readonly atlas: string;
    readonly clips: readonly SpriteClipDefinition[];
    readonly format: typeof SPRITE_ANIMATION_FORMAT;
    readonly formatVersion: number;
}

// @public
export interface SpriteAnimationEvent {
    readonly frame: number;
    readonly name: string;
}

// @public
export function spriteAnimationFileSchema(): Schema;

// @public
export interface SpriteAnimationInput {
    readonly atlas?: string;
    readonly clips: readonly SpriteClipDefinition[];
    readonly format?: string;
    readonly formatVersion?: number;
}

// @public
export function spriteAnimationJsonSchema(): JsonSchemaObject;

// @public
export class SpriteAnimator extends Component implements ComponentHooks {
    constructor();
    // @internal
    advance(deltaSeconds: number): void;
    static allowMultiple: boolean;
    animations: AssetHandle<SpriteAnimationAsset> | null;
    get asset(): SpriteAnimationAsset | null;
    get clip(): SpriteClip | null;
    defaultClip: string;
    get frame(): number;
    get isPlaying(): boolean;
    onAttach(): void;
    get onClipEnded(): Signal<string>;
    onDetach(): void;
    get onEvent(): Signal<string>;
    pause(): void;
    play(name: string, options?: PlayClipOptions): void;
    playOnAwake: boolean;
    resume(): void;
    static schema: Schema;
    speed: number;
    stop(): void;
    get time(): number;
    static typeId: string;
}

// @public
export interface SpriteAsset {
    readonly atlas: SpriteAtlasAsset;
    readonly frame: number;
    readonly name: string;
}

// @public
export class SpriteAtlasAsset {
    // @internal
    constructor(address: string, definition: SpriteAtlasDefinition, atlas: LiteSpriteAtlas | null, release: (() => void) | null);
    readonly address: string;
    static assetType: string;
    readonly definition: SpriteAtlasDefinition;
    frame(index: number): SpriteFrameInfo | null;
    get frameCount(): number;
    frameIndex(name: string): number;
    frameNames(): readonly string[];
    get isReleased(): boolean;
    get lite(): SpriteAtlasAssetLiteHandles;
    // @internal
    liteFrame(index: number): LiteSpriteFrame | null;
    // @internal
    releaseGpu(): void;
    requireFrame(name: string): number;
}

// @public
export interface SpriteAtlasAssetLiteHandles {
    readonly atlas: LiteSpriteAtlas | null;
}

// @public
export interface SpriteAtlasDefinition {
    readonly format: typeof SPRITE_ATLAS_FORMAT;
    readonly formatVersion: number;
    readonly frames: readonly SpriteFrameDefinition[];
    readonly image: string;
    readonly premultipliedAlpha: boolean;
    readonly sampling: "linear" | "nearest";
}

// @public
export function spriteAtlasFileSchema(): Schema;

// @public
export interface SpriteAtlasInput {
    readonly format?: string;
    readonly formatVersion?: number;
    readonly frames: readonly SpriteFrameDefinition[];
    readonly image: string;
    readonly premultipliedAlpha?: boolean;
    readonly sampling?: "linear" | "nearest";
}

// @public
export function spriteAtlasJsonSchema(): JsonSchemaObject;

// @public
export type SpriteBlendName = (typeof SPRITE_BLEND_MODES)[number];

// @public
export interface SpriteClip {
    readonly durationSeconds: number;
    readonly events: readonly SpriteAnimationEvent[];
    readonly fps: number;
    readonly frames: readonly number[];
    readonly loop: boolean;
    readonly name: string;
}

// @public
export interface SpriteClipDefinition {
    readonly events?: readonly SpriteAnimationEvent[];
    readonly fps?: number;
    readonly frames?: readonly string[];
    readonly from?: string;
    readonly loop?: boolean;
    readonly name: string;
    readonly to?: string;
}

// @public
export type SpriteEffectKind = (typeof SPRITE_EFFECT_KINDS)[number];

// @public
export interface SpriteFrameDefinition {
    readonly h: number;
    readonly name: string;
    readonly pivot?: Vec2Json;
    readonly sourceSize?: Vec2Json;
    readonly w: number;
    readonly x: number;
    readonly y: number;
}

// @public
export interface SpriteFrameInfo {
    readonly heightPx: number;
    readonly index: number;
    readonly name: string;
    readonly pivot: Vec2Like;
    readonly widthPx: number;
}

// @public
export class SpriteLayerEffect extends Component {
    constructor();
    static allowMultiple: boolean;
    kind: SpriteEffectKind;
    params: Vec4Like;
    static schema: Schema;
    shader: string;
    sortingLayer: string;
    source(): string;
    tint: {
        readonly r: number;
        readonly g: number;
        readonly b: number;
        readonly a: number;
    };
    static typeId: string;
    // @internal
    writeParams(out: Float32Array): Float32Array;
}

// @public
export interface SpriteLayerEntry {
    readonly count: number;
    readonly key: string;
    readonly layer: LiteSprite2DLayer;
    readonly screenSpace: boolean;
    readonly sortingLayer: string;
    readonly ySort: boolean;
}

// @public
export interface SpriteLayerKey {
    readonly atlas: SpriteAtlasAsset;
    readonly blend: SpriteBlendName;
    readonly screenSpace: boolean;
    readonly sortingLayer: string;
}

// @public
export function spriteLayerKey(sortingLayer: string, atlasAddress: string, blend: SpriteBlendName, screenSpace: boolean): string;

// @public
export class SpriteLayerRegistry {
    constructor(sortingLayers: SortingLayerTable, ySort: Readonly<Record<string, boolean>>, onLayerCreated: (layer: LiteSprite2DLayer) => void, onLayerRemoved: (layer: LiteSprite2DLayer) => void);
    // @internal
    applyOrderInLayer(key: string, handle: LiteSprite2DHandle, orderInLayer: number): void;
    // @internal
    clear(): void;
    collectLayers(worldOnly: boolean, out: LiteSprite2DLayer[]): LiteSprite2DLayer[];
    // @internal
    componentAt(layer: LiteSprite2DLayer, spriteIndex: number): SpriteRenderer | null;
    describe(): readonly SpriteLayerEntry[];
    // @internal
    forEachLayer(visit: (layer: LiteSprite2DLayer, screenSpace: boolean) => void): void;
    // Warning: (ae-forgotten-export) The symbol "SpriteScratch" needs to be exported by the entry point index.d.ts
    //
    // @internal
    place(component: SpriteRenderer, atlas: SpriteAtlasAsset, scratch: SpriteScratch): string;
    // @internal
    placeRaw(sortingLayer: string, atlas: SpriteAtlasAsset, blend: SpriteBlendName, screenSpace: boolean, scratch: SpriteScratch): {
        readonly key: string;
        readonly handle: LiteSprite2DHandle;
    };
    // @internal
    remove(component: SpriteRenderer): void;
    // @internal
    removeRaw(key: string, handle: LiteSprite2DHandle): void;
    // Warning: (ae-forgotten-export) The symbol "LiteSpriteCustomShader" needs to be exported by the entry point index.d.ts
    //
    // @internal
    setShaderProvider(provider: (sortingLayer: string) => LiteSpriteCustomShader | null): void;
    // @internal
    writeShaderParams(sortingLayer: string, params: Float32Array): void;
}

// @public
export class SpriteRenderer extends Component implements ComponentHooks {
    constructor();
    static allowMultiple: boolean;
    get atlas(): SpriteAtlasAsset | null;
    blend: SpriteBlendName;
    get bounds(): {
        readonly min: Vec2Like;
        readonly max: Vec2Like;
    };
    color: ColorLike;
    flipX: boolean;
    flipY: boolean;
    get frame(): number;
    set frame(value: number);
    // @internal
    invalidate(): void;
    get lite(): {
        readonly sprite: LiteSprite2DHandle | null;
    };
    // @internal
    markSynced(isStatic: boolean, matrixVersion: number, minX: number, minY: number, maxX: number, maxY: number): void;
    // @internal
    needsSync(isStatic: boolean, matrixVersion: number): boolean;
    onAttach(): void;
    onDetach(): void;
    orderInLayer: number;
    pickable: boolean;
    pivotOverride: Vec2Like | null;
    // @internal
    placement(): {
        readonly handle: LiteSprite2DHandle;
        readonly key: string;
    } | null;
    static schema: Schema;
    screenSpace: boolean;
    // @internal
    setPlacement(handle: LiteSprite2DHandle | null, key: string | null): void;
    sortingLayer: string;
    sprite: AssetHandle<SpriteAtlasAsset> | null;
    static typeId: string;
}

// @public
export function spriteRotationFromLite(radians: number): number;

// @public
export function spriteRotationToLite(degrees: number): number;

// @public
export interface TexturePackerImportOptions {
    readonly image?: string;
    readonly keepExtensions?: boolean;
    readonly premultipliedAlpha?: boolean;
    readonly sampling?: "linear" | "nearest";
}

// @public
export interface TileAnimationFrame {
    readonly durationMs: number;
    readonly frame: string;
}

// @public
export interface TileChange {
    readonly current: number;
    readonly layer: number;
    readonly previous: number;
    readonly x: number;
    readonly y: number;
}

// @public
export type TileColliderDefinition = {
    readonly kind: "box";
    readonly x: number;
    readonly y: number;
    readonly width: number;
    readonly height: number;
    readonly oneWay?: boolean;
} | {
    readonly kind: "polygon";
    readonly points: readonly Vec2Like[];
    readonly oneWay?: boolean;
} | {
    readonly kind: "none";
};

// @public
export interface TileCollisionInfo {
    readonly oneWay: boolean;
    readonly properties: Readonly<Record<string, string | number | boolean>>;
    readonly shape: TileCollisionShape;
}

// @public
export function tileCollisionInfo(map: TilemapDefinition, tileId: number): TileCollisionInfo;

// @public
export type TileCollisionShape = {
    readonly kind: "box";
    readonly x: number;
    readonly y: number;
    readonly width: number;
    readonly height: number;
} | {
    readonly kind: "polygon";
    readonly points: readonly Vec2Like[];
} | {
    readonly kind: "none";
};

// @public
export interface TileDefinition {
    readonly animation?: readonly TileAnimationFrame[];
    readonly collider?: TileColliderDefinition;
    readonly frame: string;
    readonly id: number;
    readonly properties?: Readonly<Record<string, string | number | boolean>>;
}

// @public
export interface TiledImportOptions {
    readonly atlasFor?: (imageSource: string) => string;
    readonly pixelsPerUnit?: number;
    readonly sortingLayer?: string;
}

// @public
export function tiledPropertiesToRecord(properties: unknown): Readonly<Record<string, string | number | boolean>>;

// @public
export function tileFrameName(map: TilemapDefinition, tileId: number): string | null;

// @public
export class Tilemap extends Component implements ComponentHooks {
    constructor();
    static allowMultiple: boolean;
    get asset(): TilemapAsset | null;
    get cellSize(): number;
    cellSizeOverride: number;
    cellToWorld<TOut extends MutableVec2>(x: number, y: number, out: TOut): TOut;
    chunkSize: number;
    collisionAt(x: number, y: number): TileCollisionInfo;
    get collisionData(): TilemapCollisionData;
    get definition(): TilemapDefinition | null;
    getTile(layer: number, x: number, y: number): number;
    // @internal
    invalidateCollision(): void;
    get layerCount(): number;
    layerSize(layer: number): Vec2Like;
    map: AssetHandle<TilemapAsset> | null;
    onAttach(): void;
    get onCollisionChanged(): Signal;
    onDetach(): void;
    get onTileChanged(): Signal<TileChange>;
    static schema: Schema;
    setTile(layer: number, x: number, y: number, tileId: number): void;
    static typeId: string;
    worldToCell<TOut extends MutableVec2>(point: Vec2Like, out: TOut): TOut;
}

// @public
export const TILEMAP_ASSET_TYPE = "tilemap";

// @public
export const TILEMAP_FILE_EXTENSIONS: readonly string[];

// @public
export const TILEMAP_FORMAT = "ignifx.tilemap";

// @public
export const TILEMAP_FORMAT_VERSION = 1;

// @public
export class TilemapAsset {
    // @internal
    constructor(address: string, definition: TilemapDefinition);
    readonly address: string;
    static assetType: string;
    readonly atlasAddresses: readonly string[];
    atlasFor(tilesetIndex: number): string;
    readonly definition: TilemapDefinition;
}

// @public
export interface TilemapCollisionChunk {
    readonly chunkX: number;
    readonly chunkY: number;
    readonly oneWayEdges: readonly (readonly [Vec2Like, Vec2Like])[];
    readonly polygons: readonly (readonly Vec2Like[])[];
}

// @public
export interface TilemapCollisionData {
    readonly cellSize: number;
    readonly chunks: readonly TilemapCollisionChunk[];
    readonly chunkSize: number;
    readonly version: number;
}

// @public
export interface TilemapDefinition {
    readonly cellSize: number;
    readonly format: typeof TILEMAP_FORMAT;
    readonly formatVersion: number;
    readonly height: number;
    readonly layers: readonly TilemapLayerDefinition[];
    readonly objects: readonly TilemapObjectDefinition[];
    readonly properties: Readonly<Record<string, string | number | boolean>>;
    readonly tileHeight: number;
    readonly tilesets: readonly TilesetDefinition[];
    readonly tileWidth: number;
    readonly width: number;
}

// @public
export function tilemapFileSchema(): Schema;

// @public
export interface TilemapInput {
    readonly cellSize?: number;
    readonly format?: string;
    readonly formatVersion?: number;
    readonly height: number;
    readonly layers?: readonly TilemapLayerInput[];
    readonly objects?: readonly TilemapObjectDefinition[];
    readonly properties?: Readonly<Record<string, string | number | boolean>>;
    readonly tileHeight?: number;
    readonly tilesets?: readonly TilesetDefinition[];
    readonly tileWidth: number;
    readonly width: number;
}

// @public
export function tilemapJsonSchema(): JsonSchemaObject;

// @public
export interface TilemapLayerDefinition {
    readonly collision: boolean;
    readonly height: number;
    readonly name: string;
    readonly opacity: number;
    readonly orderInLayer: number;
    readonly parallax: Vec2Like;
    readonly sortingLayer: string;
    readonly tiles: readonly number[];
    readonly width: number;
}

// @public
export interface TilemapLayerInput {
    readonly collision?: boolean;
    readonly height?: number;
    readonly name: string;
    readonly opacity?: number;
    readonly orderInLayer?: number;
    readonly parallax?: Vec2Like;
    readonly sortingLayer?: string;
    readonly tiles: readonly number[] | TileRleData;
    readonly width?: number;
}

// @public
export interface TilemapObjectDefinition {
    readonly height: number;
    readonly name: string;
    readonly properties: Readonly<Record<string, string | number | boolean>>;
    readonly type: string;
    readonly width: number;
    readonly x: number;
    readonly y: number;
}

// @public
export class TilemapRenderer extends Component implements ComponentHooks {
    constructor();
    // @internal
    advanceAnimation(deltaSeconds: number): boolean;
    static allowMultiple: boolean;
    // @internal
    get animationClock(): number;
    atlas: AssetHandle<SpriteAtlasAsset> | null;
    get chunkCount(): number;
    cullChunks: boolean;
    // @internal
    dropAll(registry: SpriteLayerRegistry): void;
    // @internal
    invalidate(): void;
    get loadedAtlas(): SpriteAtlasAsset | null;
    onAttach(): void;
    onDetach(): void;
    static schema: Schema;
    sortingLayer: string;
    get spriteCount(): number;
    // @internal
    sync(map: Tilemap, registry: SpriteLayerRegistry, bounds: LiteBounds2D | null, pixelsPerUnit: number): number;
    static typeId: string;
}

// @public
export interface TileObjectContext {
    readonly name: string;
    readonly position: Vec2Like;
    readonly properties: Readonly<Record<string, string | number | boolean>>;
    readonly size: Vec2Like;
    readonly tilemap: Entity;
    readonly type: string;
    readonly world: World;
}

// @public
export type TileObjectFactory = (context: TileObjectContext) => Entity | null;

// @public
export interface TileRleData {
    readonly rle: readonly number[];
}

// @public
export interface TilesetDefinition {
    readonly atlas: string;
    readonly firstId: number;
    readonly name: string;
    readonly tiles: readonly TileDefinition[];
}

// @public
export const TINT_EFFECT_WGSL = "let texel = textureSample(atlasTex, atlasSamp, uv); return vec4f(texel.rgb * fx.params.rgb, texel.a * fx.params.a);";

// @public
export const TWO_D_ANIMATION_ORDER = 0;

// @public
export const TWO_D_ERROR_MESSAGES: Readonly<Record<string, string>>;

// @public
export const TWO_D_MODES: readonly ["sprite", "mixed"];

// @public
export const TWO_D_SETTINGS_SECTION = "twoD";

// @public
export const TWO_D_SYNC_ORDER = -450;

// @public
export const twoD: (options?: TwoDOptions) => Extension;

// @public
export class TwoDAnimationSystem implements System {
    constructor(tilemaps: AnimatedTilemapSink);
    readonly name = "ignifx/2d-animation";
    update(ctx: SystemContext): void;
}

// @public
export function twoDError(code: TwoDErrorCode, message: string, options?: TwoDErrorOptions): IgnifxError;

// @public
export const TwoDErrorCode: {
    readonly atlasFrameNotExtruded: "IGX-1102";
    readonly invalidAtlasFile: "IGX-1103";
    readonly invalidAnimationFile: "IGX-1104";
    readonly invalidTilemapFile: "IGX-1105";
    readonly unknownFrame: "IGX-1106";
    readonly unknownSortingLayer: "IGX-1107";
    readonly unknownClip: "IGX-1108";
    readonly unsupportedImport: "IGX-1109";
    readonly duplicateObjectFactory: "IGX-1110";
    readonly tileOutOfRange: "IGX-1111";
    readonly duplicateExtension: "IGX-1112";
    readonly missingShaderSource: "IGX-1113";
};

// @public
export type TwoDErrorCode = (typeof TwoDErrorCode)[keyof typeof TwoDErrorCode];

// @public
export interface TwoDErrorOptions {
    readonly cause?: unknown;
    readonly context?: ErrorContext;
    readonly hint?: string;
}

// @public
export interface TwoDLiteHandles {
    readonly renderer: LiteSpriteRenderer | null;
}

// @public
export type TwoDMode = (typeof TWO_D_MODES)[number];

// @public
export interface TwoDOptions {
    readonly mode?: TwoDMode;
    readonly pixelsPerUnit?: number;
    readonly ySort?: Readonly<Record<string, boolean>>;
}

// @public
export interface TwoDPick {
    readonly component: SpriteRenderer;
    readonly entity: Entity;
    readonly u: number;
    readonly v: number;
}

// @public
export class TwoDService {
    // Warning: (ae-forgotten-export) The symbol "TwoDRuntime" needs to be exported by the entry point index.d.ts
    //
    // @internal
    constructor(runtime: TwoDRuntime);
    get layers(): readonly SpriteLayerEntry[];
    get lite(): TwoDLiteHandles;
    get mainCamera(): Camera2D | null;
    get mode(): TwoDMode;
    pickAt(xPx: number, yPx: number): TwoDPick | null;
    get pixelsPerUnit(): number;
    registerTileObjectFactory(type: string, factory: TileObjectFactory): void;
    screenToWorld(xPx: number, yPx: number, out?: MutableVec2): MutableVec2;
    get settings(): TwoDSettings;
    get sortingLayers(): readonly string[];
    get spriteCount(): number;
    get syncedLastFrame(): number;
    // @internal
    tileObjectFactory(type: string): TileObjectFactory | null;
    unregisterTileObjectFactory(type: string): boolean;
    visibleWorldBounds(out: WorldBox): boolean;
    worldToScreen(point: Vec2Like, out?: MutableVec2): MutableVec2;
}

// @public
export interface TwoDSettings {
    readonly mode: TwoDMode;
    readonly pixelsPerUnit: number;
    readonly ySort: Readonly<Record<string, boolean>>;
}

// @public
export function twoDSettingsSchema(): Schema;

// @public
export class TwoDSyncSystem implements System {
    // @internal
    constructor(runtime: TwoDRuntime);
    readonly name = "ignifx/2d-sync";
    onWorldCreated(world: World): void;
    onWorldDisposed(_world: World): void;
    update(ctx: SystemContext): void;
}

// @public
export type Vec2Json = Vec2Like | readonly [number, number];

// @public
export const VERSION = "0.0.0";

// @public
export function viewRotationToLite(degrees: number): number;

// @public
export interface WorldBox {
    readonly max: MutableVec2;
    readonly min: MutableVec2;
}

// @public
export function worldToPixelsToRef<TOut extends MutableVec2>(x: number, y: number, pixelsPerUnit: number, out: TOut): TOut;

// @public
export function zoomForSize(viewportHeightPx: number, orthographicSize: number, pixelsPerUnit: number): number;

// (No @packageDocumentation comment for this package)

```
