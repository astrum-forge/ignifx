---
name: physics
description: Adds 3D physics to an ignifx game with @ignifx/physics: Havok simulation on a separate headless scene, Rigidbody, box/sphere/capsule/mesh colliders, physics materials, triggers, CharacterController, raycasts and shape queries, the collision layer matrix, and render interpolation. Use when adding or editing 3D physics bodies, colliders, triggers, character movement, or physics queries in an ignifx project, or when the user mentions @ignifx/physics, Rigidbody, Havok, or collision layers.
license: Apache-2.0
metadata:
  ignifx-version: "0.0.0-unreleased"
---

# @ignifx/physics

> **Scaffold notice.** ignifx is in the planning phase and `@ignifx/physics` is an empty package. Every section below is filled by Phase 4 of `docs/plan/engineering-plan.md`; until then this file makes no API claims (`CONSTITUTION.md` Article V).

## What this is / when to use

`@ignifx/physics` is the ignifx extension that provides 3D physics on Havok through Babylon Lite: rigidbodies, colliders, triggers, the character controller, queries, the layer matrix, and interpolation _(populated in Phase 4)_.

## Environment

Engine version, the pinned Babylon Lite version, and the registration snippet for this extension _(populated in Phase 4)_.

## Mental model

How this subsystem's components, services, and phases fit into the App → World → Entity model _(populated in Phase 4)_.

## First app

A complete, compiling example that registers the extension and uses it once _(populated in Phase 4)_.

## Core APIs

Component and service tables generated from `skills/ignifx/references/api/physics.md` _(populated in Phase 4)_.

## Recipes

Task-oriented examples extracted from `examples/recipes/` _(populated in Phase 4)_.

## File formats

The file formats this subsystem reads and writes, with links to `skills/ignifx/references/formats/` _(populated in Phase 4)_.

## Gotchas

The traps this subsystem has, each with the replacement to use instead _(populated in Phase 4)_.

## Deprecated (current window)

None (pre-1.0: no deprecation window; breaking changes are listed in the changelog).

## Where to look next

`skills/ignifx/SKILL.md` for the engine entry skill · `docs/architecture/` for design rationale.
