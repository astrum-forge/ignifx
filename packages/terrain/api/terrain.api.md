## API Report File for "@ignifx/terrain"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { App } from '@ignifx/core';
import { AssetHandle } from '@ignifx/core';
import { AssetLoader } from '@ignifx/core';
import { ColorLike } from '@ignifx/core';
import { Component } from '@ignifx/core';
import { ComponentHooks } from '@ignifx/core';
import { ErrorContext } from '@ignifx/core';
import { Extension } from '@ignifx/core';
import { IgnifxError } from '@ignifx/core';
import { InstancedMeshRenderer } from '@ignifx/core';
import { MaterialAsset } from '@ignifx/core';
import { Mesh } from '@babylonjs/lite';
import { MeshAsset } from '@ignifx/core';
import { MutableVec3 } from '@ignifx/core';
import { Ray } from '@ignifx/core';
import { SceneNode } from '@babylonjs/lite';
import { Schema } from '@ignifx/core';
import { SchemaDescription } from '@ignifx/core';
import { ShaderAsset } from '@ignifx/core';
import { ShaderMaterialDefinition } from '@ignifx/core';
import { Signal } from '@ignifx/core';
import { System } from '@ignifx/core';
import { SystemContext } from '@ignifx/core';
import { TextureAsset } from '@ignifx/core';
import { Vec2Like } from '@ignifx/core';
import { Vec3Like } from '@ignifx/core';

// @public
export function buildChunkGeometry(field: HeightField, chunkX: number, chunkZ: number, lod: number, chunkSize: number, skirtDepth: number): ChunkGeometry;

// @public
export function chunkBounds(field: HeightField, chunkX: number, chunkZ: number, chunkSize: number, skirtDepth: number, out: Float32Array): Float32Array;

// @public
export interface ChunkGeometry {
    readonly indices: Uint32Array;
    readonly normals: Float32Array;
    readonly positions: Float32Array;
    readonly uvs: Float32Array;
}

// @public
export function chunkGridSide(chunkSize: number, lod: number): number;

// @public
export function chunkIndexCount(chunkSize: number, lod: number): number;

// @public
export function chunkVertexCount(chunkSize: number, lod: number): number;

// @public
export function controlMapCount(layerCount: number): number;

// @public
export interface ControlMaps {
    readonly maps: readonly Uint8Array[];
    readonly size: number;
}

// @public
export function createFoliageMaterial(app: App, input: FoliageMaterialInput): Promise<AssetHandle<MaterialAsset>>;

// @public
export function createHeightmapLoader(): AssetLoader<ArrayBuffer>;

// @public
export function createLayerTextures(app: App, name: string, images: TerrainLayerImages | null): TerrainLayerTextures;

// @public
export function createScatterPlacements(capacity: number): ScatterPlacements;

// @public
export function createTerrainHit(): TerrainHit;

// @public
export function createTerrainLoader(): AssetLoader<TerrainAsset>;

// @public
export interface DecodedPng {
    readonly bitDepth: 8 | 16;
    readonly channels: 1 | 2 | 3 | 4;
    readonly data: Uint8Array | Uint16Array;
    readonly height: number;
    readonly width: number;
}

// @public
export function decodePng(bytes: Uint8Array, file?: string): Promise<DecodedPng>;

// @public
export function decodeR16(buffer: ArrayBuffer, resolution: number, file?: string): Uint16Array;

// @public
export function decodeRgbaImage(bytes: ArrayBuffer, file?: string): Promise<RgbaImage>;

// @public
export function defineTerrain(input: TerrainDefinitionInput, address?: string): TerrainDefinition;

// @public
export function describeSchemas(): Readonly<Record<string, SchemaDescription>>;

// @public
export function describeTerrainFormat(): SchemaDescription;

// @public
export function distanceToBox(x: number, y: number, z: number, bounds: ArrayLike<number>, offset?: number): number;

// @public
export function encodeR16(samples: Uint16Array): ArrayBuffer;

// @public
export const FOLIAGE_SHADER_NAME = "foliage";

// @public
export function foliageMaterialDefinition(input: FoliageMaterialInput): ShaderMaterialDefinition;

// @public
export interface FoliageMaterialInput {
    readonly albedo: AssetHandle<TextureAsset>;
    readonly alphaCutoff?: number;
    readonly doubleSided?: boolean;
    readonly instanced?: boolean;
    readonly name?: string;
    readonly tint?: ColorLike;
    readonly wind?: {
        readonly strength?: number;
        readonly frequency?: number;
        readonly height?: number;
    } | null;
}

// @public
export function foliageShaderAddress(instanced?: boolean): string;

// @public
export interface FoliageShaderOptions {
    readonly instanced?: boolean;
}

// @public
export function foliageShaderSource(options?: FoliageShaderOptions): string;

// @public
export class Frustum {
    intersectsBox(minX: number, minY: number, minZ: number, maxX: number, maxY: number, maxZ: number): boolean;
    get isValid(): boolean;
    readonly planes: Float32Array;
    setFromViewProjection(m: ArrayLike<number>): boolean;
}

// @public
export function generateControlMaps(field: HeightField, layers: readonly TerrainLayerDefinition[], rules: readonly TerrainSplatRule[]): ControlMaps;

// @public
export function generateNoiseField(resolution: number, widthMetres: number, depthMetres: number, noise: TerrainNoiseDefinition, out?: Float32Array): Float32Array;

// @public
export function generateScatter(field: HeightField, control: ControlMaps, rules: ScatterRules, out: ScatterPlacements): number;

// @public
export function hashFloats(values: Float32Array): number;

// @public
export class HeightField {
    constructor(resolution: number, size: TerrainSize, heights?: Float32Array);
    assertRegion(x: number, z: number, width: number, depth: number): void;
    readonly depth: number;
    static fromNormalised(resolution: number, size: TerrainSize, values: Float32Array): HeightField;
    static fromSamples16(resolution: number, size: TerrainSize, samples: Uint16Array): HeightField;
    readonly height: number;
    heightAt(x: number, z: number): number;
    readonly heights: Float32Array;
    localToSampleX(x: number): number;
    localToSampleZ(z: number): number;
    minMax(x: number, z: number, width: number, depth: number, out: Float32Array): Float32Array;
    normalAt(x: number, z: number, out: MutableVec3): MutableVec3;
    raycast(ray: Ray, out: TerrainHit): boolean;
    readonly resolution: number;
    sample(ix: number, iz: number): number;
    sampleNormal(ix: number, iz: number, out: MutableVec3): MutableVec3;
    sampleToLocalX(ix: number): number;
    sampleToLocalZ(iz: number): number;
    setHeights(x: number, z: number, width: number, depth: number, heights: Float32Array): void;
    slopeAt(x: number, z: number): number;
    readonly spacingX: number;
    readonly spacingZ: number;
    readonly width: number;
}

// @public
export const HEIGHTMAP_ASSET_TYPE = "heightmap";

// @public
export const HEIGHTMAP_FILE_EXTENSIONS: readonly string[];

// @public
export function isPng(bytes: Uint8Array): boolean;

// @public
export const LAYERS_PER_CONTROL_MAP = 4;

// @public
export type LiteChunkMesh = Mesh;

// @public
export type LiteChunkNode = SceneNode;

// @public
export function loadGeneratedShader(app: App, source: string): Promise<AssetHandle<ShaderAsset>>;

// @public
export const LOD_HYSTERESIS = .1;

// @public
export function lodThreshold(level: number, lodDistance: number, lodBias: number): number;

// @public
export const MAX_TERRAIN_LAYERS = 8;

// @public
export function packLayerImages(albedo: readonly (RgbaImage | null)[], normals: readonly (RgbaImage | null)[], file: string): TerrainLayerImages | null;

// @public
export function pngToRgba8(png: DecodedPng): Uint8Array;

// @public
export function pngToSamples16(png: DecodedPng): Uint16Array;

// @public
export const R16_FILE_EXTENSION = ".r16";

// @public
export function resolveTerrainAddress(base: string, reference: string): string;

// @public
export interface RgbaImage {
    readonly data: Uint8Array;
    readonly height: number;
    readonly width: number;
}

// @public
export function sampleControlWeight(control: ControlMaps, layer: number, u: number, v: number): number;

// @public
export function scatterCapacity(field: HeightField, density: number, maxInstances: number): number;

// @public
export interface ScatterPlacements {
    readonly normals: Float32Array;
    readonly positions: Float32Array;
    readonly scale: Float32Array;
    readonly yaw: Float32Array;
}

// @public
export interface ScatterRules {
    readonly density: number;
    readonly heightMax: number;
    readonly heightMin: number;
    readonly layers: readonly number[];
    readonly layerThreshold: number;
    readonly maxInstances: number;
    readonly randomYaw: boolean;
    readonly scaleMax: number;
    readonly scaleMin: number;
    readonly seed: number;
    readonly slopeMax: number;
    readonly slopeMin: number;
}

// @public
export function selectLod(distance: number, lodDistance: number, lodBias: number, levels: number, current: number): number;

// @public
export function shaderSourceAddress(source: string): string;

// @public
export function solidControlMaps(size: number, layerCount: number): ControlMaps;

// @public
export class Terrain extends Component implements ComponentHooks {
    constructor();
    static allowMultiple: boolean;
    get asset(): TerrainAsset | null;
    bounds(out: Float32Array): Float32Array;
    get chunkCount(): number;
    colliderInit(region?: TerrainRegion): TerrainColliderInit;
    definition: AssetHandle<TerrainAsset> | null;
    get drawCalls(): number;
    frustumCulling: boolean;
    heightAt(x: number, z: number): number;
    get heights(): Float32Array;
    get isLoaded(): boolean;
    lodBias: number;
    lodOf(chunkX: number, chunkZ: number): number;
    get material(): AssetHandle<MaterialAsset> | null;
    normalAt(x: number, z: number, out: MutableVec3): MutableVec3;
    onAttach(): void;
    onDetach(): void;
    get onHeightsChanged(): Signal<TerrainRegion>;
    raycast(ray: Ray, out: TerrainHit): boolean;
    receiveShadows: boolean;
    regionCenter(region: TerrainRegion, out: MutableVec3): MutableVec3;
    get resolution(): number;
    sampleToWorld(ix: number, iz: number, out: MutableVec3): MutableVec3;
    static schema: Schema;
    // @internal
    selectLods(eyeX: number, eyeY: number, eyeZ: number, frustum: Frustum | null): void;
    setHeights(x: number, z: number, width: number, depth: number, heights: Float32Array): void;
    setSplat(layer: number, x: number, z: number, width: number, depth: number, weights: Uint8Array): void;
    get size(): TerrainSize;
    slopeAt(x: number, z: number): number;
    // @internal
    syncAsset(): void;
    static typeId: string;
    get visibleChunks(): number;
    worldToSample(x: number, z: number, out: Float32Array): Float32Array;
}

// @public
export const terrain: () => Extension;

// @public
export const TERRAIN_ASSET_TYPE = "terrain";

// @public
export const TERRAIN_DIAGNOSTICS_COUNTERS: readonly string[];

// @public
export const TERRAIN_DIAGNOSTICS_GROUP = "terrain";

// @public
export const TERRAIN_ERROR_MESSAGES: Readonly<Record<string, string>>;

// @public
export const TERRAIN_FILE_EXTENSIONS: readonly string[];

// @public
export const TERRAIN_FORMAT = "ignifx.terrain";

// @public
export const TERRAIN_FORMAT_VERSION = 1;

// @public
export const TERRAIN_LOD_ORDER = 10;

// @public
export const TERRAIN_SPLAT_NAME = "terrainSplat";

// @public
export class TerrainAsset {
    // Warning: (ae-forgotten-export) The symbol "TerrainAssetInit" needs to be exported by the entry point index.d.ts
    //
    // @internal
    constructor(init: TerrainAssetInit);
    readonly address: string;
    static assetType: string;
    readonly control: ControlMaps;
    readonly controlTextures: readonly AssetHandle<TextureAsset>[];
    readonly definition: TerrainDefinition;
    dispose(): void;
    readonly field: HeightField;
    get isDisposed(): boolean;
    layerIndex(name: string): number;
    readonly layers: TerrainLayerTextures;
    readonly shader: AssetHandle<ShaderAsset>;
    readonly splat: TerrainSplatShaderSpec;
}

// @public
export function terrainAssetFromDefinition(app: App, input: TerrainDefinitionInput, options?: TerrainAssetOptions): Promise<AssetHandle<TerrainAsset>>;

// @public
export interface TerrainAssetOptions {
    readonly address?: string;
    readonly heights?: Float32Array;
}

// @public
export interface TerrainChunksDefinition {
    readonly lodDistance: number;
    readonly lodLevels: number;
    readonly size: number;
    readonly skirtDepth: number;
}

// @public
export interface TerrainColliderInit {
    readonly heights: number[];
    readonly samplesX: number;
    readonly samplesZ: number;
    readonly size: Vec3Like;
}

// @public
export interface TerrainDefinition {
    readonly chunks: TerrainChunksDefinition;
    readonly chunksPerSide: number;
    readonly format: typeof TERRAIN_FORMAT;
    readonly formatVersion: typeof TERRAIN_FORMAT_VERSION;
    readonly heightmap: TerrainHeightmapDefinition | null;
    readonly layers: readonly TerrainLayerDefinition[];
    readonly material: TerrainMaterialDefinition;
    readonly name: string;
    readonly noise: TerrainNoiseDefinition | null;
    readonly resolution: number;
    readonly size: TerrainSize;
    readonly splat: TerrainSplatDefinition | null;
    readonly splatRules: readonly TerrainSplatRule[];
}

// @public
export interface TerrainDefinitionInput {
    readonly chunks?: Partial<TerrainChunksDefinition>;
    readonly format?: string;
    readonly formatVersion?: number;
    readonly heightmap?: Partial<TerrainHeightmapDefinition> | null;
    readonly layers?: readonly TerrainLayerInput[];
    readonly material?: Partial<TerrainMaterialDefinition>;
    readonly name?: string;
    readonly noise?: Partial<TerrainNoiseDefinition> | null;
    readonly resolution?: number;
    readonly size?: Partial<TerrainSize>;
    readonly splat?: {
        readonly control?: readonly string[];
    } | null;
    readonly splatRules?: readonly TerrainSplatRuleInput[];
}

// @public
export function terrainError(code: TerrainErrorCode, message: string, options?: TerrainErrorOptions): IgnifxError;

// @public
export const TerrainErrorCode: {
    readonly invalidTerrainFile: "IGX-1601";
    readonly invalidResolution: "IGX-1602";
    readonly eightBitHeightmap: "IGX-1603";
    readonly rotationUnsupported: "IGX-1604";
    readonly tooManyLayers: "IGX-1605";
    readonly unsupportedImage: "IGX-1606";
    readonly layerSizeMismatch: "IGX-1607";
    readonly heightmapSizeMismatch: "IGX-1608";
    readonly terrainNotLoaded: "IGX-1609";
    readonly regionOutOfRange: "IGX-1610";
    readonly unknownLayer: "IGX-1611";
    readonly scatterNeedsTerrain: "IGX-1612";
};

// @public
export type TerrainErrorCode = (typeof TerrainErrorCode)[keyof typeof TerrainErrorCode];

// @public
export interface TerrainErrorOptions {
    readonly cause?: unknown;
    readonly context?: ErrorContext;
    readonly hint?: string;
}

// @public
export function terrainFileSchema(): Schema;

// @public
export interface TerrainHeightmapDefinition {
    readonly source: string;
}

// @public
export interface TerrainHit {
    distance: number;
    readonly normal: MutableVec3;
    readonly point: MutableVec3;
}

// @public
export interface TerrainLayerDefinition {
    readonly albedo: string;
    readonly color: ColorLike;
    readonly name: string;
    readonly normal: string | null;
    readonly tiling: number;
    readonly triplanar: boolean;
}

// @public
export interface TerrainLayerImages {
    readonly albedo: readonly Uint8Array[];
    readonly height: number;
    readonly normals: readonly (Uint8Array | null)[];
    readonly width: number;
}

// @public
export interface TerrainLayerInput {
    readonly albedo?: string;
    readonly color?: ColorLike | readonly number[];
    readonly name: string;
    readonly normal?: string | null;
    readonly tiling?: number;
    readonly triplanar?: boolean;
}

// @public
export interface TerrainLayerTextures {
    readonly albedo: AssetHandle<TextureAsset> | null;
    readonly hasNormal: readonly boolean[];
    readonly normals: AssetHandle<TextureAsset> | null;
}

// @public
export class TerrainLodSystem implements System {
    readonly name = "ignifx/terrain-lod";
    update(ctx: SystemContext): void;
}

// @public
export interface TerrainMaterialDefinition {
    readonly metallic: number;
    readonly roughness: number;
}

// @public
export interface TerrainNoiseDefinition {
    readonly frequency: number;
    readonly lacunarity: number;
    readonly octaves: number;
    readonly persistence: number;
    readonly ridged: boolean;
    readonly seed: number;
    readonly terraces: number;
}

// @public
export interface TerrainRegion {
    readonly depth: number;
    readonly width: number;
    readonly x: number;
    readonly z: number;
}

// @public
export class TerrainScatter extends Component implements ComponentHooks {
    constructor();
    alignToNormal: boolean;
    static allowMultiple: boolean;
    get count(): number;
    density: number;
    height: Vec2Like;
    layers: string[];
    layerThreshold: number;
    lodDistance: number;
    lodMesh: AssetHandle<MeshAsset> | null;
    material: AssetHandle<MaterialAsset> | null;
    maxInstances: number;
    mesh: AssetHandle<MeshAsset> | null;
    onAttach(): void;
    onDetach(): void;
    randomYaw: boolean;
    regenerate(): void;
    static requires: readonly [typeof InstancedMeshRenderer];
    scale: Vec2Like;
    static schema: Schema;
    seed: number;
    slope: Vec2Like;
    // @internal
    syncPlacement(): void;
    static typeId: string;
}

// @public
export interface TerrainSize {
    readonly depth: number;
    readonly height: number;
    readonly width: number;
}

// @public
export interface TerrainSplatDefinition {
    readonly control: readonly string[];
}

// @public
export interface TerrainSplatLayerSpec {
    readonly color: ColorLike;
    readonly hasNormal: boolean;
    readonly tiling: number;
    readonly triplanar: boolean;
}

// @public
export interface TerrainSplatRule {
    readonly height: readonly [number, number] | null;
    readonly layer: string;
    readonly slope: readonly [number, number] | null;
}

// @public
export interface TerrainSplatRuleInput {
    readonly height?: readonly number[];
    readonly layer: string;
    readonly slope?: readonly number[];
}

// @public
export function terrainSplatShaderSource(spec: TerrainSplatShaderSpec): string;

// @public
export interface TerrainSplatShaderSpec {
    readonly layers: readonly TerrainSplatLayerSpec[];
    readonly normals: boolean;
    readonly textured: boolean;
}

// @public
export const VERSION = "0.2.1";

// @public
export function writeChunkIndices(chunkSize: number, lod: number, indices: Uint32Array): void;

// @public
export function writeChunkVertices(field: HeightField, chunkX: number, chunkZ: number, lod: number, chunkSize: number, skirtDepth: number, positions: Float32Array, normals: Float32Array, uvs?: Float32Array): void;

// (No @packageDocumentation comment for this package)

```
