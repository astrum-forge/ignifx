## API Report File for "@ignifx/particles"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { App } from '@ignifx/core';
import { AssetHandle } from '@ignifx/core';
import { AssetLoader } from '@ignifx/core';
import { ColorLike } from '@ignifx/core';
import { Component } from '@ignifx/core';
import { ComponentHooks } from '@ignifx/core';
import { CurveValue } from '@ignifx/core';
import { DiagnosticsGroup } from '@ignifx/core';
import { ErrorContext } from '@ignifx/core';
import { Extension } from '@ignifx/core';
import { IgnifxError } from '@ignifx/core';
import { InstancedMeshRenderer } from '@ignifx/core';
import { MaterialAsset } from '@ignifx/core';
import { MeshAsset } from '@ignifx/core';
import { Schema } from '@ignifx/core';
import { SchemaDescription } from '@ignifx/core';
import { ShaderAsset } from '@ignifx/core';
import { ShaderMaterialDefinition } from '@ignifx/core';
import { Signal } from '@ignifx/core';
import { StorageBufferAsset } from '@ignifx/core';
import { System } from '@ignifx/core';
import { SystemContext } from '@ignifx/core';
import { TextureAsset } from '@ignifx/core';
import { Vec2Like } from '@ignifx/core';
import { Vec3Like } from '@ignifx/core';

// @public
export function bakeCurve(curve: CurveValue): Float32Array;

// @public
export function bakeGradient(stops: readonly GradientStop[]): Float32Array;

// @public
export type ColorInput = ColorLike | readonly [r: number, g: number, b: number, a: number];

// @public
export type ColorValue = {
    readonly kind: "constant";
    readonly value: ColorLike;
} | {
    readonly kind: "random";
    readonly min: ColorLike;
    readonly max: ColorLike;
} | {
    readonly kind: "gradient";
    readonly stops: readonly GradientStop[];
    readonly samples: Float32Array;
};

// @public
export type ColorValueInput = ColorInput | {
    readonly min: ColorInput;
    readonly max: ColorInput;
} | {
    readonly gradient: readonly GradientStop[];
};

// @public
export function createParticleLoader(): AssetLoader<ParticleAsset>;

// @beta
export function createParticleState(): ParticleState;

// @public
export type DeepPartial<T> = T extends readonly unknown[] ? T : T extends object ? { readonly [K in keyof T]?: DeepPartial<T[K]>; } : T;

// @public
export function defaultParticlesSettings(): ParticlesSettings;

// @public
export function defineParticles(input: ParticleDefinitionInput, address?: string): ParticleDefinition;

// @public
export function describeParticlesFormat(): SchemaDescription;

// @public
export function describeSchemas(): Readonly<Record<string, SchemaDescription>>;

// @beta
export class EmitterRandom {
    constructor(seed: number);
    next(): number;
    reseed(seed: number): void;
    get seed(): number;
}

// @public
export function evaluateCurve(curve: CurveValue, t: number): number;

// @public
export function evaluateGradient(stops: readonly GradientStop[], t: number, out: Float32Array): Float32Array;

// @beta
export function evaluateParticle(inputs: ParticleEvaluationInputs, floats: Float32Array, words: Uint32Array, slot: number, clock: number, out: ParticleState): boolean;

// @beta
export function generateParticleWgsl(definition: ParticleDefinition): string;

// @beta
export function gradientNoise(x: number, y: number, z: number, scratch: Uint32Array): number;

// @public
export type GradientStop = readonly [t: number, r: number, g: number, b: number, a: number];

// @beta
export function hashToUnit(word: number): number;

// @public
export function isUnitScalar(value: ScalarValue): boolean;

// @public
export const LOOKUP_SAMPLES = 64;

// @public
export interface LookupRow {
    readonly index: number;
    readonly max: number;
    readonly min: number;
}

// @beta
export class MeshShapeTable {
    constructor(vertices: Float32Array);
    get area(): number;
    readonly cumulative: Float32Array;
    readonly vertices: Float32Array;
}

// @beta
export function noise3(x: number, y: number, z: number, octaves: number, scratch: Uint32Array, out: Float32Array): Float32Array;

// @public
export const PARTICLE_ASSET_TYPE = "particles";

// @public
export const PARTICLE_BLEND_MODES: readonly ["premultiplied", "additive", "alpha"];

// @public
export const PARTICLE_EMIT_FROM: readonly ["volume", "shell", "base"];

// @public
export const PARTICLE_FILE_EXTENSIONS: readonly string[];

// @public
export const PARTICLE_FRAME_MODES: readonly ["curve", "random", "fps"];

// @public
export const PARTICLE_MESHES: readonly ["box", "sphere", "plane", "cylinder", "capsule", "torus"];

// @public
export const PARTICLE_PRESETS: readonly ["fire", "smoke", "sparks", "explosion", "dust", "sparkle", "rain", "snow", "leaves"];

// @public
export const PARTICLE_RENDER_MODES: readonly ["billboard", "stretched", "horizontal", "vertical", "mesh"];

// @public
export const PARTICLE_RENDER_ORDER = 880;

// @public
export const PARTICLE_SHAPE_KINDS: readonly ["point", "sphere", "hemisphere", "cone", "box", "circle", "edge", "mesh"];

// @public
export const PARTICLE_SIMULATION_SPACES: readonly ["local", "world"];

// @public
export const PARTICLE_UPDATE_ORDER = 900;

// @public
export class ParticleAsset {
    // @internal
    constructor(address: string, definition: ParticleDefinition, texture?: AssetHandle<TextureAsset> | null, ownsTexture?: boolean);
    readonly address: string;
    static assetType: string;
    readonly definition: ParticleDefinition;
    dispose(): void;
    readonly texture: AssetHandle<TextureAsset> | null;
}

// @public
export function particleAssetFromDefinition(app: App, definition: ParticleDefinition, name?: string): AssetHandle<ParticleAsset>;

// @public
export type ParticleBlendMode = (typeof PARTICLE_BLEND_MODES)[number];

// @public
export interface ParticleBurst {
    readonly count: ScalarValue;
    readonly cycles: number;
    readonly interval: number;
    readonly probability: number;
    readonly time: number;
}

// @public
export interface ParticleBurstInput {
    readonly count?: ScalarValueInput;
    readonly cycles?: number;
    readonly interval?: number;
    readonly probability?: number;
    readonly time?: number;
}

// @public
export interface ParticleDefinition {
    readonly emission: ParticleEmission;
    readonly forces: ParticleForces;
    readonly format: typeof PARTICLES_FORMAT;
    readonly formatVersion: number;
    readonly lookup: ParticleLookup;
    readonly main: ParticleMain;
    readonly overLifetime: ParticleOverLifetime;
    readonly renderer: ParticleRenderer;
    readonly shape: ParticleShape;
    readonly start: ParticleStart;
}

// @public
export function particleDefinition(name: ParticlePreset, overrides?: DeepPartial<ParticleDefinitionInput>): ParticleDefinition;

// @public
export interface ParticleDefinitionInput {
    readonly emission?: ParticleEmissionInput;
    readonly forces?: ParticleForcesInput;
    readonly format?: string;
    readonly formatVersion?: number;
    readonly main?: ParticleMainInput;
    readonly overLifetime?: ParticleOverLifetimeInput;
    readonly renderer?: ParticleRendererInput;
    readonly shape?: ParticleShapeInput;
    readonly start?: ParticleStartInput;
}

// @public
export interface ParticleDefinitionResources {
    readonly lut: AssetHandle<TextureAsset>;
    readonly materialDefinition: ShaderMaterialDefinition;
    readonly mesh: AssetHandle<MeshAsset>;
    readonly shader: AssetHandle<ShaderAsset>;
    readonly sheet: AssetHandle<TextureAsset> | null;
    readonly source: string;
    readonly textures: readonly AssetHandle<TextureAsset>[];
}

// @public
export interface ParticleEmission {
    readonly bursts: readonly ParticleBurst[];
    readonly rateOverDistance: number;
    readonly rateOverTime: number;
}

// @public
export interface ParticleEmissionInput {
    readonly bursts?: readonly ParticleBurstInput[];
    readonly rateOverDistance?: number;
    readonly rateOverTime?: number;
}

// @public
export type ParticleEmitFrom = (typeof PARTICLE_EMIT_FROM)[number];

// @beta
export class ParticleEmitterCore {
    constructor(options: ParticleEmitterCoreOptions);
    advance(dt: number): void;
    get aliveCount(): number;
    readonly definition: ParticleDefinition;
    get drawCount(): number;
    get droppedTotal(): number;
    emit(count: number): void;
    get emittedTotal(): number;
    readonly emitterWorld: Float32Array;
    get isEmitting(): boolean;
    get isPaused(): boolean;
    get isPlaying(): boolean;
    onStopped: (() => void) | null;
    pause(): void;
    play(): void;
    qualityScale: number;
    reconcile(): RingCensus;
    resume(): void;
    readonly ring: SpawnRecordRing;
    get seed(): number;
    simulate(seconds: number): void;
    stop(options?: ParticleStopOptions): void;
    get time(): number;
}

// @beta
export interface ParticleEmitterCoreOptions {
    readonly capacity: number;
    readonly definition: ParticleDefinition;
    readonly seed: number;
}

// @beta
export interface ParticleEvaluationInputs {
    readonly definition: ParticleDefinition;
    readonly emitterWorld: Float32Array;
    readonly gravity: Float32Array;
    readonly orbitCenter: Float32Array;
}

// @public
export interface ParticleForces {
    readonly constantForce: Vec3Like;
    readonly drag: number;
    readonly gravity: Vec3Like | null;
    readonly gravityMultiplier: number;
    readonly noise: ParticleNoise | null;
    readonly orbit: ParticleOrbit | null;
}

// @public
export interface ParticleForcesInput {
    readonly constantForce?: Vec3Like;
    readonly drag?: number;
    readonly gravity?: Vec3Like | null;
    readonly gravityMultiplier?: number;
    readonly noise?: {
        readonly strength?: number;
        readonly frequency?: number;
        readonly scroll?: Vec3Like;
        readonly octaves?: number;
        readonly influenceOverLife?: ScalarValueInput;
    } | null;
    readonly orbit?: {
        readonly axis?: Vec3Like;
        readonly speed?: number;
    } | null;
}

// @public
export type ParticleFrameMode = (typeof PARTICLE_FRAME_MODES)[number];

// @public
export interface ParticleLookup {
    readonly color: LookupRow | null;
    readonly frame: LookupRow | null;
    readonly noise: LookupRow | null;
    readonly pixels: Uint8Array;
    readonly rotation: LookupRow | null;
    readonly rows: number;
    readonly size: LookupRow | null;
}

// @public
export interface ParticleMain {
    readonly capacity: number;
    readonly duration: number;
    readonly looping: boolean;
    readonly playOnAwake: boolean;
    readonly prewarm: boolean;
    readonly renderOrder: number;
    readonly seed: number;
    readonly simulationSpace: ParticleSimulationSpace;
    readonly startDelay: number;
    readonly timeScale: number;
}

// @public
export interface ParticleMainInput {
    readonly capacity?: number;
    readonly duration?: number;
    readonly looping?: boolean;
    readonly playOnAwake?: boolean;
    readonly prewarm?: boolean;
    readonly renderOrder?: number;
    readonly seed?: number;
    readonly simulationSpace?: ParticleSimulationSpace;
    readonly startDelay?: number;
    readonly timeScale?: number;
}

// @public
export type ParticleMeshName = (typeof PARTICLE_MESHES)[number];

// @public
export interface ParticleNoise {
    readonly frequency: number;
    readonly influenceOverLife: ScalarValue;
    readonly octaves: number;
    readonly scroll: Vec3Like;
    readonly strength: number;
}

// @public
export interface ParticleOrbit {
    readonly axis: Vec3Like;
    readonly speed: number;
}

// @public
export interface ParticleOverLifetime {
    readonly color: ColorValue | null;
    readonly rotation: ScalarValue | null;
    readonly size: ScalarValue | null;
    readonly sizeY: ScalarValue | null;
    readonly sizeZ: ScalarValue | null;
}

// @public
export interface ParticleOverLifetimeInput {
    readonly color?: ColorValueInput | null;
    readonly rotation?: ScalarValueInput | null;
    readonly size?: ScalarValueInput | null;
    readonly sizeY?: ScalarValueInput | null;
    readonly sizeZ?: ScalarValueInput | null;
}

// @public
export type ParticlePreset = (typeof PARTICLE_PRESETS)[number];

// @public
export function particlePresetInput(name: ParticlePreset): ParticleDefinitionInput;

// @public
export interface ParticleRenderer {
    readonly blend: ParticleBlendMode;
    readonly lengthScale: number;
    readonly lit: boolean;
    readonly mesh: ParticleMeshName;
    readonly mode: ParticleRenderMode;
    readonly pivot: Vec2Like;
    readonly sheet: ParticleSheet | null;
    readonly speedScale: number;
    readonly texture: string | null;
}

// @public
export interface ParticleRendererInput {
    readonly blend?: ParticleBlendMode;
    readonly lengthScale?: number;
    readonly lit?: boolean;
    readonly mesh?: ParticleMeshName;
    readonly mode?: ParticleRenderMode;
    readonly pivot?: Vec2Like;
    readonly sheet?: ParticleSheetInput | null;
    readonly speedScale?: number;
    readonly texture?: string | null;
}

// @public
export type ParticleRenderMode = (typeof PARTICLE_RENDER_MODES)[number];

// @beta
export class ParticleRenderSystem implements System {
    constructor(service: ParticlesService);
    readonly name: string;
    update(): void;
}

// @public
export const particles: (options?: ParticlesOptions) => Extension;

// @public
export const PARTICLES_DIAGNOSTICS_COUNTERS: readonly string[];

// @public
export const PARTICLES_DIAGNOSTICS_GROUP = "particles";

// @public
export const PARTICLES_ERROR_MESSAGES: Readonly<Record<string, string>>;

// @public
export const PARTICLES_FORMAT = "ignifx.particles";

// @public
export const PARTICLES_FORMAT_VERSION = 1;

// @public
export const PARTICLES_SETTINGS_SECTION = "particles";

// @public
export function particlesError(code: ParticlesErrorCode, message: string, options?: ParticlesErrorOptions): IgnifxError;

// @public
export const ParticlesErrorCode: {
    readonly invalidParticlesFile: "IGX-1701";
    readonly budgetExceeded: "IGX-1702";
    readonly duplicateExtension: "IGX-1703";
    readonly unknownPreset: "IGX-1704";
    readonly noDefinition: "IGX-1705";
    readonly shaderFailed: "IGX-1706";
    readonly invalidArgument: "IGX-1707";
};

// @public
export type ParticlesErrorCode = (typeof ParticlesErrorCode)[keyof typeof ParticlesErrorCode];

// @public
export interface ParticlesErrorOptions {
    readonly cause?: unknown;
    readonly context?: ErrorContext;
    readonly hint?: string;
}

// @public
export function particlesFileSchema(): Schema;

// @beta
export function particleShaderAddress(source: string): string;

// @public
export interface ParticleShape {
    readonly angle: number;
    readonly arc: number;
    readonly emitFrom: ParticleEmitFrom;
    readonly kind: ParticleShapeKind;
    readonly length: number;
    readonly radius: number;
    readonly randomDirection: number;
    readonly size: Vec3Like;
    readonly spherizeDirection: number;
    readonly thickness: number;
    readonly vertices: Float32Array;
}

// @public
export interface ParticleShapeInput {
    readonly angle?: number;
    readonly arc?: number;
    readonly emitFrom?: ParticleEmitFrom;
    readonly kind?: ParticleShapeKind;
    readonly length?: number;
    readonly radius?: number;
    readonly randomDirection?: number;
    readonly size?: Vec3Like;
    readonly spherizeDirection?: number;
    readonly thickness?: number;
    readonly vertices?: readonly number[];
}

// @public
export type ParticleShapeKind = (typeof PARTICLE_SHAPE_KINDS)[number];

// @public
export interface ParticleSheet {
    readonly fps: number;
    readonly frameOverTime: ScalarValue;
    readonly mode: ParticleFrameMode;
    readonly tiles: Vec2Like;
}

// @public
export interface ParticleSheetInput {
    readonly frameOverTime?: ScalarValueInput | "random" | {
        readonly fps: number;
    };
    readonly tiles?: Vec2Like;
}

// @public
export type ParticleSimulationSpace = (typeof PARTICLE_SIMULATION_SPACES)[number];

// @public
export interface ParticlesOptions {
    readonly gravity?: Vec3Like;
    readonly maxParticles?: number;
    readonly qualityScale?: number;
}

// @public
export class ParticlesService {
    constructor(app: App, settings: ParticlesSettings);
    // @internal
    acquireResources(asset: ParticleAsset): ParticleDefinitionResources;
    // @internal
    attach(system: ParticleSystem): void;
    get capacityInUse(): number;
    get counters(): DiagnosticsGroup | null;
    // @internal
    createMaterial(resources: ParticleDefinitionResources): AssetHandle<MaterialAsset>;
    // @internal
    detach(system: ParticleSystem): void;
    dispose(): void;
    get gravity(): Vec3Like;
    set gravity(value: Vec3Like);
    // @internal
    identitySlab(capacity: number): Float32Array;
    maxParticles: number;
    qualityScale: number;
    // @beta
    release(granted: number): void;
    // @internal
    releaseResources(asset: ParticleAsset): void;
    // @internal
    reportShaderFailure(asset: ParticleAsset, message: string): void;
    // @beta
    reserve(entity: string, requested: number): number;
    // @internal
    setCounters(group: DiagnosticsGroup): void;
    get systems(): readonly ParticleSystem[];
}

// @public
export interface ParticlesSettings {
    readonly gravity: Vec3Like;
    readonly maxParticles: number;
    readonly qualityScale: number;
}

// @public
export function particlesSettingsSchema(): Schema;

// @public
export interface ParticleStart {
    readonly color: ColorValue;
    readonly lifetime: ScalarValue;
    readonly rotation: ScalarValue;
    readonly size: ScalarValue;
    readonly size3D: Vec3Like | null;
    readonly speed: ScalarValue;
}

// @public
export interface ParticleStartInput {
    readonly color?: ColorValueInput;
    readonly lifetime?: ScalarValueInput;
    readonly rotation?: ScalarValueInput;
    readonly size?: ScalarValueInput;
    readonly size3D?: Vec3Like | null;
    readonly speed?: ScalarValueInput;
}

// @beta
export interface ParticleState {
    age: number;
    readonly color: Float32Array;
    frame: number;
    life: number;
    readonly position: Float32Array;
    rotation: number;
    readonly size: Float32Array;
    readonly velocity: Float32Array;
}

// @public
export interface ParticleStopOptions {
    readonly clear?: boolean;
}

// @public
export class ParticleSystem extends Component implements ComponentHooks {
    constructor();
    get aliveCount(): number;
    static allowMultiple: boolean;
    get asset(): ParticleAsset | null;
    get capacity(): number;
    // @internal
    get core(): ParticleEmitterCore | null;
    definition: AssetHandle<ParticleAsset> | null;
    get drawCount(): number;
    get droppedCount(): number;
    emit(count: number): void;
    // @internal
    get emittedThisFrame(): number;
    // @beta
    evaluate(slot: number, out?: ParticleState): boolean;
    get isPaused(): boolean;
    get isPlaying(): boolean;
    // @internal
    get material(): AssetHandle<MaterialAsset> | null;
    onAttach(): void;
    onDetach(): void;
    readonly onStopped: Signal<ParticleSystem>;
    pause(): void;
    play(): void;
    playOnAwake: boolean;
    // @beta
    get records(): Float32Array | null;
    // @internal
    get renderer(): InstancedMeshRenderer | null;
    resume(): void;
    static schema: Schema;
    seed: number;
    simulate(seconds: number): void;
    stop(options?: ParticleStopOptions): void;
    // @internal
    get storage(): AssetHandle<StorageBufferAsset> | null;
    // @internal
    sync(): void;
    // @internal
    tick(dt: number): void;
    get time(): number;
    static typeId: string;
    // @internal
    get uploadedBytes(): number;
}

// @beta
export function particleUnits(seed: number, out: Float32Array, scratch: Uint32Array): Float32Array;

// @beta
export class ParticleUpdateSystem implements System {
    constructor(service: ParticlesService);
    readonly name: string;
    update(ctx: SystemContext): void;
}

// @beta
export function pcg3d(x: number, y: number, z: number, out: Uint32Array): Uint32Array;

// @beta
export function readScalarRow(definition: ParticleDefinition, row: LookupRow | null, life: number, channel: number, fallback: number): number;

// @beta
export const RECORD_BYTES: number;

// @beta
export const RECORD_FLOATS = 12;

// @beta
export const RECORD_LIFETIME = 1;

// @beta
export const RECORD_POSITION = 4;

// @beta
export const RECORD_ROTATION = 11;

// @beta
export const RECORD_SEED = 2;

// @beta
export const RECORD_SIZE = 7;

// @beta
export const RECORD_SPAWN_TIME = 0;

// @beta
export const RECORD_VELOCITY = 8;

// @beta
export function recordSeed(systemSeed: number, index: number, scratch: Uint32Array): number;

// @public
export function resolveColor(value: ColorValue, unit: number, t: number, out: Float32Array): Float32Array;

// @public
export function resolveScalar(value: ScalarValue, unit: number, t: number): number;

// @beta
export interface RingCensus {
    alive: number;
    drawCount: number;
}

// @public
export function sampleRow(samples: Float32Array, t: number): number;

// @beta
export function sampleShape(shape: ParticleShape, rng: EmitterRandom, mesh: MeshShapeTable | null, position: Float32Array, direction: Float32Array): void;

// @public
export function scalarMax(value: ScalarValue): number;

// @public
export function scalarMin(value: ScalarValue): number;

// @public
export type ScalarValue = {
    readonly kind: "constant";
    readonly value: number;
} | {
    readonly kind: "random";
    readonly min: number;
    readonly max: number;
} | {
    readonly kind: "curve";
    readonly curve: CurveValue;
    readonly samples: Float32Array;
};

// @public
export type ScalarValueInput = number | {
    readonly min: number;
    readonly max: number;
} | {
    readonly curve: CurveValue;
};

// @beta
export class SpawnRecordRing {
    constructor(capacity: number);
    readonly capacity: number;
    clear(): void;
    readonly floats: Float32Array;
    get head(): number;
    markUploaded(): void;
    get pendingCount(): number;
    pendingRanges(out: [UploadRange, UploadRange]): number;
    reconcile(clock: number, out: RingCensus): RingCensus;
    slotOf(index: number): number;
    get tail(): number;
    readonly words: Uint32Array;
    write(spawnTime: number, lifetime: number, seed: number, flags: number, px: number, py: number, pz: number, size: number, vx: number, vy: number, vz: number, rotation: number): number;
    get written(): number;
}

// @public
export function srgbToLinear(value: number): number;

// @beta
export interface UploadRange {
    readonly count: number;
    readonly start: number;
}

// @public
export const VERSION = "0.2.1";

// (No @packageDocumentation comment for this package)

```
