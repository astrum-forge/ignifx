// The package's own version, reported as `Extension.version`. The release tooling bumps it and
// `package.json` together.

/**
 * The `@ignifx/particles` version this build was cut from.
 *
 * @public
 */
export const VERSION = "0.2.1";
