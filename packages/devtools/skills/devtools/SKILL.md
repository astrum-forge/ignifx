---
name: devtools
description: Inspects and debugs a running ignifx game with @ignifx/devtools: the stats and diagnostics overlay, the scene tree browser, the schema-driven component inspector, the physics debug view, scene reload, and script hot reload. Use when diagnosing frame time, entity counts, component values, or physics behaviour in an ignifx project, or when the user mentions @ignifx/devtools, the stats overlay, the inspector, or hot reload.
license: Apache-2.0
metadata:
  ignifx-version: "0.0.0-unreleased"
---

# @ignifx/devtools

> **Scaffold notice.** ignifx is in the planning phase and `@ignifx/devtools` is an empty package. Every section below is filled by Phase 10 of `docs/plan/engineering-plan.md`; until then this file makes no API claims (`CONSTITUTION.md` Article V).

## What this is / when to use

`@ignifx/devtools` is the ignifx extension that provides the stats overlay, scene tree and component inspector, physics debug view, and script hot reload _(populated in Phase 10)_.

## Environment

Engine version, the pinned Babylon Lite version, and the registration snippet for this extension _(populated in Phase 10)_.

## Mental model

How this subsystem's components, services, and phases fit into the App → World → Entity model _(populated in Phase 10)_.

## First app

A complete, compiling example that registers the extension and uses it once _(populated in Phase 10)_.

## Core APIs

Component and service tables generated from `skills/ignifx/references/api/devtools.md` _(populated in Phase 10)_.

## Recipes

Task-oriented examples extracted from `examples/recipes/` _(populated in Phase 10)_.

## File formats

The file formats this subsystem reads and writes, with links to `skills/ignifx/references/formats/` _(populated in Phase 10)_.

## Gotchas

The traps this subsystem has, each with the replacement to use instead _(populated in Phase 10)_.

## Deprecated (current window)

None (pre-1.0: no deprecation window; breaking changes are listed in the changelog).

## Where to look next

`skills/ignifx/SKILL.md` for the engine entry skill · `docs/architecture/` for design rationale.
