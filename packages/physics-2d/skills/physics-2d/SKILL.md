---
name: physics-2d
description: Adds 2D physics to an ignifx game with @ignifx/physics-2d: Rigidbody2D, 2D colliders, CharacterController2D, one-way platforms, 2D triggers, and 2D queries, layered on the @ignifx/2d toolkit. Use when adding or editing 2D physics bodies, 2D colliders, platformer movement, or 2D queries in an ignifx project, or when the user mentions @ignifx/physics-2d, Rigidbody2D, or CharacterController2D.
license: Apache-2.0
metadata:
  ignifx-version: "0.0.0-unreleased"
---

# @ignifx/physics-2d

> **Scaffold notice.** ignifx is in the planning phase and `@ignifx/physics-2d` is an empty package. Every section below is filled by Phase 6 of `docs/plan/engineering-plan.md`; until then this file makes no API claims (`CONSTITUTION.md` Article V).

## What this is / when to use

`@ignifx/physics-2d` is the ignifx extension that provides 2D physics: `Rigidbody2D`, 2D colliders, `CharacterController2D`, and queries _(populated in Phase 6)_.

## Environment

Engine version, the pinned Babylon Lite version, and the registration snippet for this extension _(populated in Phase 6)_.

## Mental model

How this subsystem's components, services, and phases fit into the App → World → Entity model _(populated in Phase 6)_.

## First app

A complete, compiling example that registers the extension and uses it once _(populated in Phase 6)_.

## Core APIs

Component and service tables generated from `skills/ignifx/references/api/physics-2d.md` _(populated in Phase 6)_.

## Recipes

Task-oriented examples extracted from `examples/recipes/` _(populated in Phase 6)_.

## File formats

The file formats this subsystem reads and writes, with links to `skills/ignifx/references/formats/` _(populated in Phase 6)_.

## Gotchas

The traps this subsystem has, each with the replacement to use instead _(populated in Phase 6)_.

## Deprecated (current window)

None (pre-1.0: no deprecation window; breaking changes are listed in the changelog).

## Where to look next

`skills/ignifx/SKILL.md` for the engine entry skill · `docs/architecture/` for design rationale.
