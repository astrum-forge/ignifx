/**
 * The package's own version, reported as `Extension.version`
 * (`docs/architecture/04-extensions.md` §1). First-party packages ship one version line, so this
 * tracks `package.json` and the release tooling bumps both together.
 */

/**
 * The `@ignifx/ui` version this build was cut from.
 *
 * @public
 */
export const VERSION = "0.0.0";
