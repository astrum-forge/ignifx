---
name: ui
description: Builds game UI in ignifx with @ignifx/ui: the DOM overlay host and layers, scaling modes, input focus routing, world-space anchors, world and HUD text, virtual joysticks and buttons for touch, dialogs, toasts, loading screens, and localization. Use when adding or editing menus, HUDs, on-screen text, touch controls, or overlay UI in an ignifx project, or when the user mentions @ignifx/ui, HudText, WorldAnchor, or VirtualJoystick.
license: Apache-2.0
metadata:
  ignifx-version: "0.0.0-unreleased"
---

# @ignifx/ui

> **Scaffold notice.** ignifx is in the planning phase and `@ignifx/ui` is an empty package. Every section below is filled by Phase 8 of `docs/plan/engineering-plan.md`; until then this file makes no API claims (`CONSTITUTION.md` Article V).

## What this is / when to use

`@ignifx/ui` is the ignifx extension that provides the DOM overlay UI host, input focus routing, and world and HUD text components _(populated in Phase 8)_.

## Environment

Engine version, the pinned Babylon Lite version, and the registration snippet for this extension _(populated in Phase 8)_.

## Mental model

How this subsystem's components, services, and phases fit into the App → World → Entity model _(populated in Phase 8)_.

## First app

A complete, compiling example that registers the extension and uses it once _(populated in Phase 8)_.

## Core APIs

Component and service tables generated from `skills/ignifx/references/api/ui.md` _(populated in Phase 8)_.

## Recipes

Task-oriented examples extracted from `examples/recipes/` _(populated in Phase 8)_.

## File formats

The file formats this subsystem reads and writes, with links to `skills/ignifx/references/formats/` _(populated in Phase 8)_.

## Gotchas

The traps this subsystem has, each with the replacement to use instead _(populated in Phase 8)_.

## Deprecated (current window)

None (pre-1.0: no deprecation window; breaking changes are listed in the changelog).

## Where to look next

`skills/ignifx/SKILL.md` for the engine entry skill · `docs/architecture/` for design rationale.
