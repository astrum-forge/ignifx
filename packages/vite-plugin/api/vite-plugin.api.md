## API Report File for "@ignifx/vite-plugin"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ConfigEnv } from 'vite';
import { Plugin as Plugin_2 } from 'vite';

// @public
export function addressFromRelativePath(relativePath: string): string;

// @public
export function appendPointer(pointer: string, segment: string): string;

// @public
export const ASSET_CHANGED_EVENT = "ignifx:asset-changed";

// @public
export const ASSET_MANIFEST_FORMAT = "ignifx.manifest";

// @public
export const ASSET_MANIFEST_FORMAT_VERSION = 1;

// @public
export const ASSET_TYPE_BY_EXTENSION: {
    readonly ".png": "texture";
    readonly ".jpg": "texture";
    readonly ".jpeg": "texture";
    readonly ".webp": "texture";
    readonly ".ktx2": "texture";
    readonly ".basis": "texture";
    readonly ".glb": "model";
    readonly ".gltf": "model";
    readonly ".env": "environment";
    readonly ".hdr": "environment";
    readonly ".dds": "environment";
    readonly ".ttf": "font";
    readonly ".otf": "font";
    readonly ".mp3": "audio";
    readonly ".ogg": "audio";
    readonly ".wav": "audio";
    readonly ".json": "json";
    readonly ".txt": "text";
    readonly ".md": "text";
    readonly ".csv": "text";
    readonly ".bin": "binary";
    readonly ".wasm": "binary";
};

// @public
export const ASSET_TYPE_BY_SUFFIX: {
    readonly ".scene.json": "scene";
    readonly ".prefab.json": "scene";
    readonly ".material.json": "material";
    readonly ".input.json": "inputactions";
    readonly ".audio.json": "audiobuses";
    readonly ".physicsmaterial.json": "physicsmaterial";
    readonly ".i18n.json": "i18n";
    readonly ".animator.json": "animator";
    readonly ".atlas.json": "spriteatlas";
    readonly ".spriteanim.json": "spriteanimation";
    readonly ".tilemap.json": "tilemap";
};

// @public
export interface AssetChangedPayload {
    readonly address: string;
    readonly kind: "added" | "changed" | "removed";
    readonly url: string;
}

// @public
export interface AssetManifest {
    readonly entries: readonly AssetManifestEntry[];
    readonly format: typeof ASSET_MANIFEST_FORMAT;
    readonly formatVersion: typeof ASSET_MANIFEST_FORMAT_VERSION;
    readonly root: string;
}

// @public
export interface AssetManifestEntry {
    readonly address: string;
    readonly bytes: number;
    readonly groups: readonly string[];
    readonly hash: string;
    readonly meta?: JsonObject;
    readonly type: AssetType;
    readonly url: string;
}

// @public
export type AssetType = (typeof ASSET_TYPE_BY_SUFFIX)[keyof typeof ASSET_TYPE_BY_SUFFIX] | (typeof ASSET_TYPE_BY_EXTENSION)[keyof typeof ASSET_TYPE_BY_EXTENSION];

// @public
export function assetTypeForAddress(address: string): AssetType;

// @public
export function buildManifest(assets: readonly ScannedAsset[], root: string, urlFor: (asset: ScannedAsset) => string): AssetManifest;

// @public
export function collectExtensionPublicAssets(root: string): Promise<readonly ExtensionPublicAsset[]>;

// @public
export const DEFAULT_ASSET_ROOT = "assets";

// @public
export const DEFAULT_ASSET_TYPE = "binary";

// @public
export const DEFAULT_HASH_LENGTH = 8;

// @public
export const DEFAULT_MANIFEST_FILE_NAME = "assets.manifest.json";

// @public
export const DEFAULT_PUBLIC_PATH = "assets/";

// @public
export const DEFAULT_SCRIPTS_PATTERN = "src/scripts/**/*.ts";

// @public
export function defineConfig<T>(config: T): T;

// @public
export const EXTENSION_KEYWORD = "ignifx-extension";

// @public
export interface ExtensionPublicAsset {
    readonly fileName: string;
    readonly filePath: string;
    readonly packageName: string;
}

// @public
export function findIgnifxConfigFile(root: string): Promise<string | null>;

// @public
export interface FormatHeader {
    readonly format: string;
    readonly formatVersion: number;
}

// @public
export function formatValidationProblem(problem: ValidationProblem): string;

// @public
export function hashedAddress(address: string, hash: string): string;

// @public
export function ignifx(options?: IgnifxPluginOptions): Plugin_2<IgnifxPluginApi>;

// @public
export const IGNIFX_CONFIG_DEFINE_KEY = "import.meta.env.IGNIFX_CONFIG";

// @public
export const IGNIFX_CONFIG_FILE_NAMES: readonly ["ignifx.config.ts", "ignifx.config.mts", "ignifx.config.js", "ignifx.config.mjs"];

// @public
export interface IgnifxPluginApi {
    whenIdle(): Promise<void>;
}

// @public
export interface IgnifxPluginOptions {
    readonly assetRoot?: string;
    readonly config?: string | false;
    readonly hashLength?: number;
    readonly manifestFileName?: string;
    readonly publicPath?: string;
    readonly schemas?: JsonSchemaProvider;
    readonly scripts?: string;
    readonly validate?: boolean;
}

// @public
export function isJsonArray(value: JsonValue): value is JsonArray;

// @public
export function isJsonObject(value: JsonValue): value is JsonObject;

// @public
export function isSidecarFileName(fileName: string): boolean;

// @public
export type JsonArray = readonly JsonValue[];

// @public
export type JsonObject = {
    readonly [key: string]: JsonValue;
};

// @public
export function jsonProperty(object: JsonObject, key: string): JsonValue | undefined;

// @public
export type JsonSchemaObject = JsonObject;

// @public
export interface JsonSchemaProvider {
    readonly formats?: Readonly<Record<string, JsonSchemaObject>>;
    readonly scene?: JsonSchemaObject;
}

// @public
export type JsonValue = string | number | boolean | null | JsonArray | JsonObject;

// @public
export function loadIgnifxConfig(configFile: string | null, root: string, env: ConfigEnv): Promise<ResolvedIgnifxConfig>;

// @public
export const MANIFEST_MODULE_ID = "virtual:ignifx/manifest";

// @public
export function manifestModuleSource(manifest: AssetManifest): string;

// @public
export function normalizeScriptsPattern(pattern: string): string;

// @public
export function parseJsonValue(text: string): JsonValue;

// @public
export const PLUGIN_NAME = "ignifx";

// @public
export function requiresFormatHeader(address: string): boolean;

// @public
export const RESOLVED_MANIFEST_MODULE_ID: string;

// @public
export const RESOLVED_SCRIPTS_MODULE_ID: string;

// @public
export interface ResolvedIgnifxConfig {
    readonly config: JsonObject;
    readonly dependencies: readonly string[];
    readonly path: string | null;
}

// @public
export interface ResolvedIgnifxPluginOptions {
    readonly assetRoot: string;
    readonly configFile: string | null | false;
    readonly hashLength: number;
    readonly manifestFileName: string;
    readonly publicPath: string;
    readonly schemas: JsonSchemaProvider | null;
    readonly scriptsPattern: string;
    readonly validate: boolean;
}

// @public
export function resolvePluginOptions(options: IgnifxPluginOptions): ResolvedIgnifxPluginOptions;

// @public
export function resolveVirtualModuleId(id: string): string | null;

// @public
export function scanAssetRoot(options: ScanAssetRootOptions): Promise<readonly ScannedAsset[]>;

// @public
export interface ScanAssetRootOptions {
    readonly assetRoot: string;
    readonly hashLength?: number;
}

// @public
export interface ScannedAsset {
    readonly address: string;
    readonly bytes: number;
    readonly filePath: string;
    readonly groups: readonly string[];
    readonly hash: string;
    readonly meta: JsonObject | null;
    readonly type: AssetType;
}

// @public
export interface SchemaViolation {
    readonly message: string;
    readonly pointer: string;
}

// @public
export const SCRIPTS_HOT_RELOAD_EXPORT = "acceptHotReload";

// @public
export const SCRIPTS_MODULE_ID = "virtual:ignifx/scripts";

// @public
export interface ScriptsModuleOptions {
    readonly hot?: boolean;
}

// @public
export function scriptsModuleSource(pattern: string, options?: ScriptsModuleOptions): string;

// @public
export function serializeManifest(manifest: AssetManifest): string;

// @public
export function splitAssetFileName(fileName: string): {
    readonly stem: string;
    readonly extension: string;
};

// @public
export function validateJsonAsset(address: string, filePath: string, text: string, schemas: JsonSchemaProvider | null): readonly ValidationProblem[];

// @public
export function validateJsonAssets(assets: readonly ScannedAsset[], schemas: JsonSchemaProvider | null): Promise<readonly ValidationProblem[]>;

// @public
export interface ValidateJsonOptions {
    readonly maxDepth?: number;
    readonly root?: JsonSchemaObject;
}

// @public
export function validateJsonValue(value: JsonValue, schema: JsonSchemaObject, options?: ValidateJsonOptions): readonly SchemaViolation[];

// @public
export interface ValidationProblem {
    readonly address: string;
    readonly code: VitePluginErrorCode;
    readonly filePath: string;
    readonly message: string;
    readonly pointer: string;
}

// @public
export class VitePluginError extends Error {
    constructor(code: VitePluginErrorCode, message: string, options?: ErrorOptions);
    readonly code: VitePluginErrorCode;
}

// @public
export const VitePluginErrorCode: {
    readonly assetRootMissing: "IGX-0550";
    readonly invalidSidecar: "IGX-0551";
    readonly duplicateOutputFile: "IGX-0552";
    readonly extensionAssetMissing: "IGX-0553";
    readonly invalidProjectConfig: "IGX-0554";
    readonly invalidOption: "IGX-0555";
    readonly malformedJson: "IGX-0650";
    readonly missingFormatHeader: "IGX-0651";
    readonly schemaViolation: "IGX-0652";
    readonly unsupportedSchema: "IGX-0653";
};

// @public
export type VitePluginErrorCode = (typeof VitePluginErrorCode)[keyof typeof VitePluginErrorCode];

// (No @packageDocumentation comment for this package)

```
