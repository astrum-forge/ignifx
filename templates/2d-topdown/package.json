{
  "name": "ignifx-template-2d-topdown",
  "version": "0.0.0",
  "private": true,
  "description": "ignifx template: a top-down 2D game with a tilemap, a character controller, input schemes, and audio.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "out/main/index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "dev:desktop": "electron-vite dev",
    "build:desktop": "electron-vite build",
    "dist:desktop": "electron-builder --dir --config electron-builder.yml",
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.desktop.json"
  },
  "dependencies": {
    "@ignifx/2d": "workspace:*",
    "@ignifx/audio": "workspace:*",
    "@ignifx/core": "workspace:*",
    "@ignifx/input": "workspace:*",
    "@ignifx/physics-2d": "workspace:*",
    "@ignifx/ui": "workspace:*"
  },
  "devDependencies": {
    "@ignifx/electron": "workspace:*",
    "@ignifx/vite-plugin": "workspace:*",
    "@webgpu/types": "catalog:",
    "electron": "catalog:",
    "electron-builder": "catalog:",
    "electron-vite": "catalog:",
    "typescript-native": "catalog:",
    "vite": "catalog:"
  }
}
