@AGENTS.md

Claude-specific notes:

- The engine usage skill is discoverable at `.claude/skills/ignifx` (symlink to `skills/ignifx`).
- When delegating research or drafting to subagents, verify their claims against primary sources before merging them into `docs/`.
