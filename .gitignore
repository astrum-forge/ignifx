node_modules/
dist/
coverage/
.turbo/
*.tsbuildinfo
.tsbuild/
.tsbuild-test/

# API Extractor scratch output (only api/*.api.md is committed)
temp/
etc/temp/

# Electron desktop build output: `out/` is what `electron-vite build` writes and `release/` is what
# `electron-builder` writes (a 300 MB unpacked application, for `--dir` on macOS).
out/
release/

# Playwright / Vitest browser output
test-results/
playwright-report/
blob-report/
.playwright/
.vitest/

# Local editor and OS noise
.DS_Store
*.local
.vscode/*
!.vscode/extensions.json
