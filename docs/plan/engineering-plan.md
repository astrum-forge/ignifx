# ignifx Engineering Plan

**Status:** v1 (2026-09-05) · **Owner:** Astrum Forge Studios · **Precedence:** below constitution, standards, architecture docs, ADRs (`CONSTITUTION.md` §10.1) · **Living document** (`CONSTITUTION.md` §7.6)

This plan takes ignifx from an empty repository to a 1.0 release. It is ordered so that every phase produces something runnable and testable, each phase's riskiest unknown is retired by a spike before the phase's API is frozen, and documentation ships with code rather than after it.

---

## 1. Assumptions

- **Team:** one lead engineer (owner) coordinating AI coding agents, with occasional contributors. Agents draft; humans approve (`CONSTITUTION.md` §7.4). Estimates are in engineer-weeks of coordinated effort and carry ±40% uncertainty until Phase 2 calibrates them.
- **Review bandwidth** is the scarcest resource: every merge needs a human approval (`CONSTITUTION.md` §7.4), and physics, rendering, and determinism code takes disproportionate review time per line. Each phase budgets approver-hours next to its estimate (rule of thumb: 25% of engineer-weeks), and a phase does not start while the previous phase's review queue is open.
- **Foundation facts** (verified 2026-09-05): `@babylonjs/lite` 1.27.0; TypeScript 7.0.2 (native) with `@typescript/typescript6` for API-dependent tools; Node 24 LTS; pnpm 11; Vite 8; Vitest 5; Electron 44. See `docs/standards/coding-standards.md` §1.
- **Scope of 1.0:** the packages in `docs/architecture/00-overview.md` §2, four reference templates (2D top-down, 2D side-scroller, 3D third-person, 3D first-person), the Agent Skill and docs harness, the public website shell, Electron support. No editor, no networking, no XR before 1.0 (`00-overview.md` §7).
- **Cadence:** trunk-based, releasable `main`, a `0.x` minor release at the end of each phase that changes public API.

## 2. Milestone map

| Phase | Name                                             | Outcome                                                                                                             | Release | Estimate                 |
| ----- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- | ------- | ------------------------ |
| 0     | Foundation                                       | Repo, toolchain, CI, docs harness skeleton, empty packages, website shell                                           | —       | 1–2 wk                   |
| 1     | Kernel                                           | App/World/Entity/Component/Script, Transform on Lite nodes, Time and phases, signals, coroutines, headless stepping | 0.1     | 3–4 wk                   |
| 2     | Rendering and assets                             | Camera/Light/MeshRenderer/Model/Environment/materials on Lite; asset service and loaders; scene format v1; prefabs  | 0.2     | 4–5 wk                   |
| 3     | Input                                            | Action maps, devices, pointer lock, rebinding                                                                       | 0.3     | 2 wk                     |
| 4     | Physics 3D                                       | Havok on the simulation scene, colliders, triggers, character controller, queries, interpolation                    | 0.4     | 3–4 wk                   |
| 5     | Audio                                            | Buses, sources, listener, clips, music player                                                                       | 0.5     | 2 wk                     |
| 6     | 2D toolkit + 2D physics                          | Camera2D, sprites, animation, atlases, tilemaps, parallax, Rapier 2D, two 2D templates                              | 0.6     | 4–5 wk                   |
| 7     | 3D toolkit                                       | Character/camera rigs, Animator, navigation, two 3D templates                                                       | 0.7     | 4–5 wk                   |
| 8     | UI                                               | DOM overlay host, anchors, world/HUD text, virtual controls, localization                                           | 0.8     | 2 wk                     |
| 9     | Platform                                         | Storage, Electron package, desktop templates, browser fallback page                                                 | 0.9     | 2–3 wk                   |
| 10    | Devtools                                         | Overlay panels, inspector, hot reload                                                                               | 0.9.x   | 2–3 wk                   |
| 11    | Docs harness and skill completion                | Generated references, recipes, subsystem skills, llms.txt, drift gates on                                           | 0.9.x   | 2–3 wk (plus continuous) |
| 12    | Templates, examples, website content, benchmarks | Polished templates, example gallery, site content, baselines                                                        | 0.9.x   | 3 wk                     |
| —     | Hardening and API freeze                         | Two consecutive minors without breaking changes, budgets met, browser matrix green                                  | 1.0     | 4–6 wk                   |

Phase 5 (audio) can run in parallel with Phase 4; Phase 3 (input) must finish before Phases 6 and 8, whose exit criteria need gamepad/touch actions and focus suppression; Phase 8 can then run in parallel with Phase 7. See §6. Total to 1.0: roughly 40–50 engineer-weeks.

## 3. Phase details

Each phase lists **Spikes** (retire risk first), **Deliverables**, **Exit criteria** (all must be true and verified in CI, except criteria explicitly marked _manual_, which are recorded in a tracking issue), and **Docs**.

### Phase 0 · Foundation (1–2 wk)

Spikes

- S0.1 Toolchain proof: a hello package builds with `tsdown` (`isolatedDeclarations`), type-checks with TS 7 `tsc --build`, lints with Oxlint + tsgolint, and runs a Vitest browser test in Chromium with WebGPU that calls `createEngine` on an `OffscreenCanvas`/canvas (headless Chrome with `--enable-unsafe-webgpu --use-angle=vulkan --enable-features=Vulkan` or SwiftShader).
- S0.2 TS6 alias proof: API Extractor and TypeDoc run against the hello package through the `typescript` → `@typescript/typescript6` alias (ADR-0007).

Deliverables

- Root config: `package.json`, `pnpm-workspace.yaml` (catalog with exact `@babylonjs/lite@1.27.0`, TS alias pair, `allowBuilds`), `turbo.json`, `tsconfig.base.json`, `lefthook.yml`, `commitlint.config.ts`, `renovate.json`, `.nvmrc` (24), `.editorconfig`.
- Lint/format: `.oxlintrc.json`, `eslint.config.ts` (gap rules + placeholders for the custom `ignifx/*` rules, implemented in Phase 1), `.oxfmtrc`.
- Empty packages with barrels, `api/` reports, `skills/<name>/SKILL.md` stubs: `core`, `input`, `physics`, `physics-2d`, `audio`, `2d`, `3d`, `ui`, `electron`, `devtools`, `vite-plugin`, `cli`, `ignifx`.
- CI: `ci.yml` (all jobs from standards §12, some no-op), `release.yml` (Changesets + Trusted Publishing, dry-run), `website.yml`.
- Docs harness skeleton: `docs:api`, `docs:schemas`, `docs:recipes` scripts (empty output), example-extraction checker, skill lint; `.claude/skills/ignifx` symlink.
- `website/`: Vite + TypeScript SPA shell, placeholder page, `public/llms.txt`, separate deploy (design deferred).
- `create-ignifx` CLI skeleton that copies a template directory.
- `AGENTS.md`, `CLAUDE.md`, `CONTRIBUTING.md`, PR template with the definition-of-done checklist.

Exit criteria

- `pnpm check` passes on a clean clone in under 3 minutes; CI green on all jobs; a tagged `0.0.1` dry-run publish produces valid packages (publint, arethetypeswrong).
- Browser test job proves WebGPU is available in CI (software adapter acceptable).

Docs: standards table updated with the exact pinned versions; ADR-0009 confirmed.

Status (2026-09-05): delivered on `main`. Met locally: `pnpm check` green in 15 s, WebGPU browser tests pass in Chromium (macOS; Linux flags unverified, see ADR-0009), dependency layering clean, dry-run publish of all 14 packages passes publint and arethetypeswrong. Outstanding until the first push: CI green on GitHub, the tagged `0.0.1` dry run, the npm scope claim (§9.1).

### Phase 1 · Kernel (3–4 wk) → 0.1

Spikes

- S1.1 Transform on Lite nodes: create `TransformNode`s, parent them by direct `parent`/`children` linking and by `setParent`, read `worldMatrix`/`worldMatrixVersion`, confirm that pure transform nodes need not be added to the Lite scene and that a mesh parented under them renders (Phase 2 confirms visually; Phase 1 verifies matrices numerically headless).
- S1.2 Single-callback loop: register one `onBeforeRender` after extensions; verify callback order relative to a `createHavokWorld` registration (physics callback must not run inside the render scene).
- S1.3 Headless determinism: `app.step(1/60)` × N produces identical transforms across runs and across Node/Chromium.

Deliverables (`@ignifx/core`)

- `createApp`, `App` (start/stop/dispose/step, services, settings, `onError`, diagnostics), extension host (`04-extensions.md`), core extension.
- `Time`, scheduler with phases, fixed loop, lifecycle flushes, destroy queue, coroutines (`01-lifecycle-and-time.md`).
- `World`, `SceneInstance` (implicit default scene), `Entity`, `Transform`, `Component`, `Script`, `Component.define` schema kinds, registration and `typeId`s, `Signal`, tags and layers, `LayerMask`.
- Math module (`Vec2/3/4`, `Quat`, `Mat4`, `Color`, `MathUtils`) structurally compatible with Lite's interfaces, with `ToRef` variants.
- Errors (`IgnifxError`, codes 01xx–04xx), logging, diagnostics core.
- Custom lint rules `ignifx/*` (standards §6.5).
- Vitest projects (node + browser) with fake clock utilities.

Exit criteria

- Lifecycle order test suite encodes every row of `01-lifecycle-and-time.md` §3–§6 and passes headless.
- Determinism test (S1.3) passes; coroutine semantics tests pass; 90% coverage on core.
- Two apps can run in one process (no globals).
- API report and generated `references/api/core.md` committed; entry `SKILL.md` documents `createApp`, `Entity`, `Script`, `Time`, `Signal` with compiling examples.

Status (2026-09-05): delivered on `main`. Met locally: the lifecycle suites (`packages/core/test/lifecycle/**`) encode the frame order and the activation/enabling/destruction rules of `01-lifecycle-and-time.md` §3–§6 and pass headless; the S1.3 determinism test reproduces hash `7b79fbda` across Node and Chromium (ADR-0003 Validation, `packages/core/test/determinism/**`); the coroutine semantics suite passes; `pnpm test` runs 1,132 tests in 79 files with 97.5% line and 92.1% branch coverage, over the 90% core floor `vitest.config.ts` enforces; `packages/core/test/app/app.test.ts` proves two apps run independently in one process; `packages/core/api/core.api.md` and the generated `skills/ignifx/references/api/core.md` are committed, and the entry `SKILL.md` documents `createApp`, `Entity`, `Script`, `Time`, and `Signal` with examples the documentation harness compiles. Outstanding until CI runs: every exit criterion re-verified on GitHub Actions (Linux WebGPU flags still unverified, ADR-0009), and the browser project on a non-macOS runner.

### Phase 2 · Rendering and assets (4–5 wk) → 0.2

Spikes

- S2.1 Camera-as-child: a Lite `FreeCamera` parented under an entity node follows the entity's transform; orthographic toggle works; viewport mapping verified against `resolveCameraViewport`.
- S2.2 Runtime adds after `registerScene`: measure frames-to-visible for a new material family and for a pre-registered one; decide default warm-up behaviour.
- S2.3 Feature opt-ins: shadows via `registerSceneWithShadowSupport`, `enableDeviceLostSceneRecovery` ordering; forced device loss (`forceWebGpuDeviceLossForTesting`) recovers a simple scene.
- S2.4 Asset pipeline: `.glb` fetched as bytes with progress + abort into `loadGltf`; `cloneTransformNode` instancing shares GPU buffers (memory measured).

Deliverables

- Lite adapter: engine creation with capability check, render scene, `Camera`, `Light` (+ shadow generators), `MeshRenderer`, `Model`, `Environment`, `MaterialAsset` (PBR/Standard/Shader), `PostProcessStack` (bloom, SMAA, image processing), picking, device-loss wiring, screenshots, diagnostics (`07-rendering.md`).
- Asset service: addresses, handles, refcounts, priority queue, cancellation, manifest, core loaders (texture, model, scene, material, environment, font, json/text/binary), hot reload hooks (`05-assets-and-loading.md`).
- `@ignifx/vite-plugin`: manifest generation, `.meta.json`, JSON Schema validation, public asset copying, HMR channel.
- Serialization: scene format v1, instances/overrides, `serializeScene`, JSON Schema generation (`06-serialization-and-scene-format.md`).
- `examples/hello-cube`, `examples/gltf-viewer`; first visual golden tests.
- `benchmarks/` seed: hello-cube and a 1,000-entity scene with frame-time **and heap-growth** baselines (bounded heap growth over 600 headless steps enforces the allocation-free rule of the standards).

Exit criteria

- Visual tests: hello cube, PBR model with IBL and shadows, orthographic camera, post-process stack — goldens committed.
- Scene round-trip test: load → serialize → load produces byte-identical output; instance overrides applied and re-derived.
- Asset tests: refcount to zero unloads; abort cancels; hot reload swaps a texture in the browser test.
- Device-loss recovery test passes for the basic scene; documented unrecoverable cases produce `onDeviceRecoveryFailed`.
- Bundle-size baseline for `examples/hello-cube` recorded (target: ignifx core + Lite minimal scene under 250 KB gzipped; revisit after measurement).
- Skill: `references/formats/scene.md`, `material.md`, recipes "load a model", "spawn a prefab".

Status (2026-09-06): delivered on `main`. Met locally: the scene round trip is byte-identical through save → load → save (`packages/core/test/serialization/round-trip.test.ts`), and instance overrides are applied and re-derived (`instances.test.ts`, `overrides.test.ts`); the asset suites cover refcount-to-zero unloading after `gcDelay`, `AbortSignal` cancellation (`IGX-0502`), delivery at the `PreUpdate` point, and an in-place hot-reload swap firing `onReplaced` (`packages/core/test/assets/**` — the swap is proved at the service level in the node project; the browser texture-swap case is not yet a separate test); device-loss recovery has browser tests at both layers (`packages/core/test/lite/render/device-loss.browser.test.ts`, `packages/core/test/render/device-loss.browser.test.ts`) with the unrecoverable cases reported through `app.events.onDeviceRecoveryFailed`; ADR-0014 records the S2.2 warm-up measurement and `warm-up.browser.test.ts` pins it. Skill: `references/formats/` carries the hand-written `scene.md` and `material.md` beside the generated `ignifx.scene.md`, `ignifx.material.md`, `ignifx.environment.md`, `components.md`, and `ignifx.schemas.json` (nine schemas), and the two recipes are generated from `examples/recipes/load-a-model` and `examples/recipes/spawn-a-prefab`; every fenced `ts` block in `skills/**` compiles under `pnpm docs:harness`. Note on naming: a generated formats page takes its name from the format id, so the scene and material _field tables_ are `ignifx.scene.md`/`ignifx.material.md` and the exit criterion's `scene.md`/`material.md` are the prose pages beside them. Visual goldens: four scenes (`hello-cube`, `hello-cube-orthographic`, `gltf-viewer`, `gltf-viewer-post`) committed under `tests/visual/tests/__screenshots__/` with per-scene tolerances 0.02–0.05, one image per scene shared across platforms (Linux SwiftShader unverified). The suite exposed four render defects that were fixed before the commit and are recorded in ADR-0002 (post-processing must render through an offscreen target and is now the `rendering.features.postProcessing` opt-in; Lite lights are no longer parented; `Model` shadows; `rendering.clearColor`). Benchmarks (`benchmarks/baselines.json`): heap growth over 600 headless steps is bounded (≤ 104 KB for 4 entities, 9 KB for 1,002 — allocation-free frames hold); 1,002 entities step in 0.16 ms. Bundle-size baseline: the `examples/hello-cube` entry chunk is 270,551 B gzipped against the plan's 250 KB target — NOT met, 20.5 KB over; the asserted ceiling is 298,000 B and the target is revisited in Phase 12. Outstanding until CI runs: every exit criterion re-verified on GitHub Actions, and the browser project on a non-macOS runner.

### Phase 3 · Input (2 wk) → 0.3

Deliverables (`@ignifx/input`): devices, action maps, bindings/composites/processors, control schemes, pointer lock, cursor, `.input.json` loader, rebinding, `PlayerInput`, headless simulation API (`08-input.md`).

Exit criteria: frame-consistency tests (edge flags stable across fixed steps), composite/processors unit tests, rebinding round trip, gamepad mapping table tests, browser test with synthetic DOM events; skill subsystem file `packages/input/skills/input/SKILL.md`.

Status (2026-09-06): delivered on `main`. Met locally: the frame-consistency suite (`packages/input/test/actions/frame-consistency.test.ts`) proves `wasPressedThisFrame` is `true` inside every one of a frame's five fixed steps and `false` in the next frame, and that `fixedUpdate`, `update`, and `lateUpdate` read identical values; every composite and processor has numeric unit tests including the radial deadzone, `normalize`, and `clamp` (`test/bindings/**`); the rebinding round trip covers `performInteractiveRebind` through `simulate`, highest-magnitude disambiguation, exclusions, cancel path, timeout, and `saveOverrides` → `loadOverrides` → identical bindings (`test/service/rebinding.test.ts`); the gamepad mapping table is tested for the standard layout and for two non-standard remaps (`test/devices/gamepad.test.ts`); and the Chromium suite dispatches real `KeyboardEvent`, `PointerEvent`, and `WheelEvent` objects at a canvas and at the window on a real `createApp({ canvas })` app, covering the pointer-lock request path and the cursor style (`test/input.browser.test.ts`). `pnpm test` runs 1,335 tests with the input package at 96.2% lines / 86.0% branches, over its 80% floor; `pnpm test:browser` runs 9 input tests in Chromium. The subsystem skill `packages/input/skills/input/SKILL.md` (233 lines) ships with the generated `references/formats/ignifx.inputactions.md` and the hand-written `inputactions.md` beside it, and every fenced `ts` block compiles under `pnpm docs:harness`.

Decisions this phase settled, none of which `08-input.md` fixed: the device-slot syntax is `<Gamepad>{1}/leftStick` with a **zero-based** slot index, so one number means one thing across `gamepads[i]`, `PlayerInput.deviceSlot`, and binding paths; `Mouse`, `Pointer`, and `Touch` are all fed from DOM **pointer** events rather than from `touch*`, because a touch already fires `pointerdown` with its own `pointerId` and subscribing to both would double-count; a binding's `scheme` tag filters device pairing and UI glyphs but **not** resolution, unless `input.strictSchemes` is set (Unity's semantics); and `PlayerInput` exposes its private lookup as `playerInput.input` because the serialized field already owns the name `actions`. Two `08-input.md` stretch goals are **not** shipped: the `hold`, `tap`, and `multiTap` interactions. Outstanding until CI runs: every exit criterion re-verified on GitHub Actions, and the browser project on a non-macOS runner.

### Phase 4 · Physics 3D (3–4 wk) → 0.4

Spikes

- S4.1 Simulation scene: Havok world on a null-engine scene stepped by `stepScene` while meshes render in the render scene; determinism across runs; Havok WASM served through the manifest.
- S4.2 Collision body identities: attempt upstream PR `onPhysicsCollisionBodies`; in parallel prototype the adapter-internal drain with a layout guard test. Decide by end of week 1; if still undecided, ship the drain behind the ADR-0013 opt-in flag, document trigger events as the default gameplay mechanism, and revisit at 0.4.1 (R-4).
- S4.3 Interpolation: display-pose write/restore does not leak into Havok (kinematic and dynamic cases).

Deliverables (`@ignifx/physics`): service, `Rigidbody`, colliders, `PhysicsMaterial`, `CharacterController`, layer matrix, queries, events, interpolation, debug viewer hook, settings (`09-physics.md`).

Exit criteria: analytic free-fall test (Lite's own headless example) reproduced through ignifx; stacking stability scene; trigger and collision enter/stay/exit tests; character controller slope/step and interpolation tests; determinism test on three platforms in CI (Linux, macOS, and Windows runners); forced device loss with active bodies and coroutines recovers with the simulation intact; physics subsystem skill.

Status (2026-09-06): delivered on `main`. Met locally: the analytic free-fall test (`packages/physics/test/physics/free-fall.test.ts`) reproduces Lite's headless example through ignifx and records a Havok integrator detail — the first step integrates about 0.61 of one gravity increment and every later step exactly `g·h²` — so the assertion bounds the position against the analytic curve and pins the per-step increment; the stacking scene holds five boxes; trigger and collision enter/stay/exit are delivered to both entities (`events.test.ts`), collision identities through the ADR-0013 drain behind `physics({ collisionIdentities: "internal" })` with the `IGX-0908` layout guard (`test/lite/collision-drain.test.ts`); the character controller climbs 30° and refuses 60°, steps 0.3 m, and snaps to ground (`character.test.ts`); interpolation restore is proved against an uninterpolated twin over 40 irregular frames (`interpolation.test.ts`); the determinism suite reproduces hash `2301e7d4` across two apps, interleaved runs, and three processes on macOS arm64 (`determinism/`); the browser suite forces a device loss with active bodies and a running coroutine and asserts the simulation continued and the body landed (`test/lite/browser.browser.test.ts`). `pnpm test` runs the physics package's 99 node tests at 93.8% lines / 80.9% branches, over its 80% floor; `pnpm test:browser` runs 4 physics tests in Chromium. The subsystem skill `packages/physics/skills/physics/SKILL.md` (248 lines) ships with the generated `references/formats/ignifx.physicsmaterial.md`, and every fenced `ts` block compiles under `pnpm docs:harness`. Spikes: S4.1 retired (simulation scene on a null engine, WASM served at the bare address `HavokPhysics.wasm` through the manifest, Node falls back to the installed package); S4.2 decided — the upstream path does not exist in Lite 1.27.0, so the drain ships behind the opt-in and ADR-0013 is activated; S4.3 retired. Kernel and build fixes this phase surfaced: `setSimulationScene(null)` tolerated during dispose, and `ignifx.assets.public` paths under `node_modules/` resolved through ancestor directories for hoisting package managers. `09-physics.md` carries twelve listed corrections (collision events as a three-valued enum, per-body velocity limits removed, root-entity rule `IGX-0907`, heightfield sample grid, `MeshCollider` GPU-only, broadcast collision delivery, `overlap` from the extension's own bounds index, no `lazy` option). NOT met until CI runs: the determinism hash on Linux and Windows runners, and the browser project on a non-macOS runner. Deliberate substitution: the device-loss test asserts the simulation continued rather than comparing against a headless twin, because a rAF-driven app and a headless one cannot be step-aligned.

### Phase 5 · Audio (2 wk) → 0.5

Deliverables (`@ignifx/audio`): service, buses from `.audio.json`, `AudioClip` loader (static/streaming), `AudioSource`, `AudioListener`, `MusicPlayer`, unlock flow, pause behaviour, headless stub (`10-audio.md`).

Exit criteria: browser tests with an `OfflineAudioContext` (Lite supports it) for bus routing and spatial attenuation; unlock flow test with a synthetic gesture; headless timing test for `onEnded`; audio skill.

Status (2026-09-06): delivered on `main`. Met locally: the Chromium suite (`packages/audio/test/lite/web-backend.browser.test.ts`) renders through an `OfflineAudioContext` and measures bus-gain routing and spatial attenuation from the rendered buffer; the unlock flow is tested at the service layer with the locked state, queued plays, and the release on `unlock()` (`test/service/unlock.test.ts`), and in Chromium against a real `AudioContext` resumed by a synthetic gesture; `onEnded` timing is exact under the headless backend because simulated playback advances by the frame delta (`test/headless/headless-backend.test.ts`, `test/components/audio-source.test.ts`). `pnpm test` runs the audio package's 150 node tests at 97.8% lines (excluding `src/lite/web/**`, the browser-only half of the adapter, which the browser project covers), and `pnpm test:browser` runs 13 audio tests in Chromium. The subsystem skill `packages/audio/skills/audio/SKILL.md` (321 lines) ships with the generated `references/formats/ignifx.audiobuses.md`, and every fenced `ts` block compiles under `pnpm docs:harness`. Decisions this phase settled: `.mp3`/`.ogg`/`.webm`/`.flac` clips report `duration: null` under Node, so headless timing tests use `.wav`; a Web Audio route is fixed at creation, so changing `clip`, `bus`, `loop`, `maxInstances`, or `spatial` on a playing source rebuilds the voice; `app.pause()` pauses every bus but `UI` by default (`audio({ pauseWithApp: false })` opts out). Outstanding until CI runs: the browser project on a non-macOS runner.

### Phase 6 · 2D toolkit and 2D physics (4–5 wk) → 0.6

Spikes

- S6.1 Sprite sync at scale: 10,000 static tiles + 1,000 moving sprites in Lite layers at 60 fps; chunking strategy for tilemaps.
- S6.2 Rapier 2D: bundle impact (`-compat` inline vs. served `.wasm`), determinism, character controller behaviour on slopes/one-way platforms (ADR-0006 confirmation).
- S6.3 Pixel-perfect: integer zoom + DPR strategy produces crisp output on 1×, 1.5×, 2× displays (visual goldens).

Deliverables (`@ignifx/2d`, `@ignifx/physics-2d`): `Camera2D`, `SpriteRenderer`, atlases and importers (TexturePacker, Aseprite, grid), `SpriteAnimator`, `Tilemap` + renderer + collider with Tiled/LDtk importers, `ParallaxLayer`, sorting layers, Y-sort, 2D picking, layer effects; `Rigidbody2D`, colliders 2D, `CharacterController2D`, queries, events; templates `2d-topdown` and `2d-sidescroller` (`11-2d-toolkit.md`).

Exit criteria: both templates playable with keyboard, gamepad, and touch; visual goldens; tilemap import tests for Tiled and LDtk fixtures; 2D physics determinism and controller tests; performance budget met (S6.1 numbers recorded as baselines); 2D and physics-2d skills.

Status (2026-09-06): delivered on `main`. Met locally: `@ignifx/2d` (410 node tests at 94.6% lines, 22 Chromium tests) ships `Camera2D`, `SpriteRenderer`, `SpriteAnimator`, atlases with the TexturePacker/Aseprite/grid importers, `Tilemap`/`TilemapRenderer` with the Tiled and LDtk importers, `ParallaxLayer`, `SpriteLayerEffect`, sorting layers with Y-sort, pixel-perfect cameras, and 2D picking; `@ignifx/physics-2d` (92 node tests at 94.4% lines, 1 Chromium test) ships Rapier 2D bodies, colliders, triggers, `CharacterController2D`, the layer matrix (first sixteen layers, `IGX-1152` above), queries, events, and interpolation, with the determinism suite reproducing hash `5718c5f0` across two apps and three processes on macOS arm64. S6.1 (`benchmarks/baselines.json` → `twoD`, asserted by `benchmarks/two-d-baselines.test.ts`): sprite sync for 1,000 movers over a 100×100 tilemap costs 0.30 ms median per frame in Chromium and 0.2448 ms in Node against the 2 ms budget, with 0 static sprites synced while idle; S6.2 (ADR-0006 Validation): `@dimforge/rapier2d-compat` stays — its inlined WebAssembly costs 794.5 KB gzipped against about 643 KB for the served variant, under the 300 KB threshold for switching — and the build is same-machine deterministic only, so the cross-platform claim was removed from ADR-0006 and `11-2d-toolkit.md` §8; S6.3: 0 blended pixels at DPR 1, 1.5 and 2 with `pixelPerfect`, 4,096 without. `templates/2d-topdown` and `templates/2d-sidescroller` build, typecheck, and are playable with keyboard, gamepad and touch bindings (touch through an in-template `<Virtual>` widget until `@ignifx/ui` ships `VirtualJoystick` in Phase 8), with visual goldens at 512×288 and tolerance 0.02 (`tests/visual/tests/templates.spec.ts`), and a headless play-test of both levels (slopes, one-way planks, coyote time, prop collisions) drove two level and controller fixes before the goldens were taken. `create-ignifx --template 2d-*` copies a template and rewrites `workspace:` ranges. Decisions: 2D physics uses the 3D callback names (the kernel's `PhysicsCallbackName` has no `2D` variants); `CharacterController2D` gained `shape: "capsule" | "box"` because Rapier's autostep needs a box; `IGX-1101` is registered by `@ignifx/physics-2d` and `@ignifx/2d` starts at `IGX-1102`; the 2D sync system runs at `PreRender −450`. Bundle: both templates' entry chunks are about 1.13 MB gzipped, 797 KB of which is Rapier's inlined WebAssembly (the non-Rapier remainder is in `hello-cube`'s range); ceilings are recorded at ×1.1 and the plan's 250 KB target is revisited in Phase 12, where a dynamic import of Rapier is the obvious first move. NOT met until CI runs: the browser project, the goldens, and the determinism hash on non-macOS runners; neither touch nor gamepad hardware was exercised. Follow-ups recorded: a `renderer.onSceneRegistered` signal (the sprite renderer is created lazily on the first `PreRender` tick), skipping the 3D scene pass in `"sprite"` mode, a validated per-scene settings mechanism, the `IGX-0706` warning at `"sprite"`-mode start-up, and a kerb tile that exercises autostep in the side-scroller.

### Phase 7 · 3D toolkit (4–5 wk) → 0.7

Spikes

- S7.1 Animator on ignifx clock: detach animation groups from a loaded model, drive with `updateAnimationManager`, blend two clips with `crossFadeAnimationGroups`, masks with `createAnimationGroupMask`; verify `lateUpdate` sees the posed skeleton.
- S7.2 Navigation: Recast WASM via `createNavigationPluginAsync`, crowd update in `FixedUpdate`, agent positions written to transforms.

Deliverables (`@ignifx/3d`): `ThirdPersonController`, `FirstPersonController`, `ThirdPersonCamera`, `Animator` (asset + state machine + events + blend trees 1D), tweens (core), `NavMeshSurface`/`NavMeshAgent`/`NavMeshObstacle`, `LodGroup`, `Billboard`; templates `3d-third-person` and `3d-first-person` (`12-3d-toolkit.md`).

Exit criteria: templates playable; animator state-machine unit tests (transitions, exit time, triggers, events); navigation path tests headless; camera collision test; 3D skill. `CameraBrain`/`VirtualCamera` are stretch (7b) and may slip to 0.9.x.

Status (2026-09-06): delivered on `main`. Met locally: `@ignifx/3d` (189 node tests at 92.7% lines, 5 Chromium tests) ships `ThirdPersonController`, `FirstPersonController`, `RigidbodyMover`/`PlatformMover`/`Projectile`, `ThirdPersonCamera` with wall collision, the `Animator` (state machine, 1D blend trees, masks, additive layers, events from `.animator.json`) on a Lite `AnimationManager` driven by ignifx's clock, `NavMeshSurface`/`NavMeshAgent`/`NavMeshObstacle` with `app.navigation` on Recast, `LodGroup`, and `Billboard`; core gained `app.tweens` (`12-3d-toolkit.md` §4). The state-machine suite covers transitions, exit time, triggers consumed once, `any`-state transitions, interruptibility, blend-tree weights and events once per loop; the camera collision test pulls the boom in against a wall and eases it back; the navigation suite (17 cases) runs **headlessly** — S7.2 found Recast's WebAssembly inlined as a data URL in Lite's vendor chunk (452 KB gzipped, code-split and fetched only when a surface bakes; ADR-0017) — including path-around-a-wall, arrival, seeded determinism and tile-cache obstacles; S7.1 proved from rendered frames that the skeleton is posed by the `Animator`, differs between clips, and holds exactly still under `app.pause()`. Findings recorded in `12-3d-toolkit.md`: `createAnimationManager` needs an `engine`; `playAnimation` is mandatory (a loaded glTF marks every clip but the first stopped, silently); one `Animator` per model asset, because Lite binds a group to one manager and clones share the skeleton; Lite has no navmesh serialization, so `.navmesh.bin`/`ignifx bake navmesh` are not implementable yet, and no `removeAgent`. `templates/3d-third-person` and `templates/3d-first-person` build, typecheck, and are playable with keyboard, gamepad and touch bindings on `physics()` + `input()` + `audio()` + `threeD()` + `ui()`: third-person has the controller, the collision-filtered boom, the `Animator` on the generated rig (`tests/fixtures/assets/3d/rig.glb`, 10 KB, 4 clips) driving the idle/walk/run blend tree and the `jump` trigger, a `NavMeshAgent` companion over a surface baked from the level's own geometry, seeded `Rigidbody` crates and a footstep one-shot; first-person has pointer lock on the first click, crouch, a view model with a prop welded through `Model.attachToNode("hand", …)`, an `Interactable`-masked raycast that recolours a pedestal, and an `AudioListener` on the head. Headless play-tests scored 16/16 each (walk, sprint, a 1.16 m jump for a 1.1 m `jumpHeight`, wall collision, boom 4.50 → 0.53 → 4.50 m, the companion closing 5.48 → 1.97 m around walls, 200 px of mouse yawing the body 24.0° while pitching only the head, the crosshair ray toggling a pedestal). Visual goldens at 512×288, tolerance 0.05 (`tests/visual/tests/templates.spec.ts`); entry chunks 406,181 and 405,904 B gzipped with ceilings at ×1.1 — a third of a 2D template's, because Havok arrives as a copied `.wasm` and Recast as a lazy chunk while Rapier 2D is inlined base64. Follow-ups recorded: after landing, a character rests a stable 8.9 cm above its standing height with `verticalVelocity` held at the ground-stick speed (3D/physics); `ThirdPersonCamera` and `FirstPersonController` integrate look without `dt`, so stick look is frame-rate dependent and the templates scale `<Gamepad>/rightStick` in `.input.json` instead; `NavMeshSurface.addSource` needs clockwise-from-outside winding or Recast fails outright (documented in the template); `@ignifx/input` loses a key pressed and released inside one frame (edges are derived once per frame from the resolved value); the `ignifx/no-entity-find-in-src` rule false-positives on `actions.find("…")` (resolved 2026-09-07: `allowedReceivers` option, `**/scripts/**` exclude removed); `SpriteAnimator` is not yet driven by `Animator`. NOT met until CI runs: the goldens and browser suites on a non-macOS runner; neither touch nor gamepad hardware was exercised; `CameraBrain`/`VirtualCamera` (7b) are not started.

### Phase 8 · UI (2 wk) → 0.8

Deliverables (`@ignifx/ui`): overlay host and layers, scaling modes, focus routing, `WorldAnchor`, `WorldText`/`WorldText2D`/`HudText` on Lite text, `VirtualJoystick`/`VirtualButton`, `Dialog`/`Toast`/`LoadingScreen`, `app.i18n` (`13-ui.md`).

Exit criteria: focus routing test (typing in a field does not fire actions); anchor placement visual test; text rendering golden; templates use the loading screen and a pause menu; UI skill.

Status (2026-09-06): delivered on `main`. Met locally: `@ignifx/ui` (192 node tests at 98.0% lines, 20 Chromium tests) ships `app.ui` (overlay root beside the canvas, named layers with derived z-indexes, `"css"`/`"fit"`/`"dpi"` scaling, safe-area CSS variables, focus routing), `WorldAnchor`, `HudText`/`WorldText`/`WorldText2D` on Lite's text renderer, `Dialog`/`Toast`/`LoadingScreen`, `VirtualJoystick`/`VirtualButton` feeding `@ignifx/input`'s virtual device, and `app.i18n` with the `ignifx.i18n` format (all locales in one `.i18n.json`, ICU-style plurals). Exit criteria: the focus-routing browser test mounts a real `<input>` in a layer, focuses it, dispatches the bound key and asserts the action stays released, then fires after `blur()`; the anchor-placement test asserts an anchored element's centre within 1 px of `Camera.worldToScreen`; the text golden is a deterministic `captureScreenshot` assertion (lit pixels inside the predicted glyph box, none in the opposite corner) rather than a stored image; all four templates open a `LoadingScreen` bound to `app.assets` before their first load and carry a pause `Dialog` toggled by a `pause` action (Escape / Start / `<Virtual>/pause`) from a script with `static updateWhenPaused = true`, and the two 2D templates deleted their hand-rolled touch widget for `VirtualJoystick`/`VirtualButton` without changing a binding (`13-ui.md` §3's axis convention held); `3d-third-person` exercises `app.i18n` with two locales through `?locale=fr`. Decisions recorded in `13-ui.md`: only text-entry elements set `app.input.uiHasFocus` (a focused `<button>` keeps gameplay running); a press on a `pointer-events: auto` element never reaches the canvas, and for drags the host now writes `app.input.uiHasPointer` (added to `@ignifx/input` this phase) so pointing-device actions read as released; the host is absent, not hidden, headlessly; `"dpi"` means one UI unit per backing-store pixel. Engine fixes this phase surfaced: pointer positions and deltas are now backing-store pixels (the space `Camera.worldToScreen`, `screenToRay` and `pickAsync` always used — the "CSS pixel" claims were wrong at DPR ≠ 1); 2D sprite animation now freezes under `app.pause()` (systems keep running with a non-zero `dt`; the contract is now written into `01-lifecycle-and-time.md` §7); the Vite plugin discovers symlinked packages, which is every pnpm-installed package, so extension public assets such as Havok's `.wasm` reach a build. Lite gaps reported: `addTextRenderable` is one-shot (a `WorldText` created after `app.start()` never draws — `IGX-1308`; use `WorldText2D` mid-game) and there is no `removeTextRenderable`. The fixture font `ShareTechMono-Regular.ttf` (OFL-1.1, 43 KB, provenance in `tests/fixtures/assets/ui/ATTRIBUTION.md`) is used by tests only; the templates use DOM HUDs, because redistributing an OFL font inside a template needs the owner's sign-off. NOT met until CI runs: the browser suites on a non-macOS runner; no real touch, notch, or high-DPI hardware was exercised; text device-loss recovery is wired but not exercised.

### Phase 9 · Platform and Electron (2–3 wk) → 0.9

Spikes

- S9.1 Electron 44 + WebGPU on macOS, Windows, Linux (flags, `ignifx://` protocol serving the manifest, pointer lock, gamepads); document per-OS caveats.

Deliverables: `app.platform`, `app.storage` backends (IndexedDB, Electron FS, memory/Node), `@ignifx/electron` (main/preload/renderer), `create-ignifx --desktop` variants of the four templates, electron-vite + electron-builder configs, browser fallback page (`14-platform-electron.md`).

Exit criteria: desktop templates build installers on three OSes in CI (electron-builder, unsigned); storage round-trip tests per backend; save/load round trip through `serializeScene` + `app.storage` including instanced entities with overrides and a prefab-hash mismatch test; forced device loss in a running desktop template recovers with physics, coroutines, and audio intact; security checklist test (window options asserted); electron skill.

Status (2026-09-06): delivered on `main`. Met locally: `app.platform` carries the full `14-platform-electron.md` §1 surface (`kind`, `os`, `isMobile`, `hasPointerLock`, `hasGamepads`, `webgpu` from the adapter or `null`, `locale`, `reducedMotion`) with `platformInternals(...).setKind("electron")` as the one mutation; `app.storage` implements §2 over a frozen `StorageBackend` contract with three backends — memory (headless default), a Node directory backend with atomic renames and a reversible key codec, and IndexedDB (browser default; one object store keyed `namespace/key`) — plus the Electron file-system backend bridged over IPC; storage round-trip tests run per backend (memory and file in Node, IndexedDB in Chromium, Electron through the bridge in the Playwright suite); the save/load round trip serialises a scene with an instanced prefab carrying overrides through `serializeScene` → `app.storage` → a fresh app and re-serialises byte-identically, and the prefab-hash mismatch test pins `06-serialization-and-scene-format.md` §2's existing rule (`IGX-0604` logged and best-effort by default, thrown under `strictInstanceHashes`). `@ignifx/electron` ships `createGameWindow` (main), the sandboxed CommonJS preload bridge (`window.ignifxHost`, `invoke`-based, versioned), the `ignifx://app` protocol serving the manifest and hashed assets with `Range` support, and the renderer `electron()` extension (`platform.kind`, file-system storage, `app.desktop`); the **security checklist** is asserted twice — from the pure `windowOptionsFor`/`cspFor` builders and at runtime inside Electron — for `contextIsolation`, `sandbox`, `nodeIntegration: false`, `webSecurity`, the strict CSP header, denied navigation/`window.open`/permissions except pointer lock and fullscreen, and an `https:`/`mailto:` allow-list for `openExternal`; the **device-loss** exit criterion passes in a running desktop build of `3d-third-person` (`deviceLost 1, deviceRecovered 1`, coroutine ticking, crate at rest, audio `running`), and the four templates now ship ESM shadows because Lite 1.27.0 can rebuild only ESM shadow maps after a lost device (their 3D goldens were retaken). `create-ignifx --desktop` copies a template with its `desktop/` variant; every template has `dev:desktop`/`build:desktop`/`dist:desktop` on electron-vite 6.0.0-beta.1 (the first release declaring Vite 8; probed, ADR-0018) and electron-builder, and `dist:desktop` produced an unsigned macOS arm64 `.app` (306 MB, `app.asar` 18.9 MB). The Electron Playwright suite runs 11 tests on macOS: secure origin, WebGPU adapter and rendered frame, runtime security options, bridge-only global, CSP refusing inline script, manifest and `Range` → 206, `platform.kind`, storage through the bridge, device loss, and the `openExternal` refusal (`IGX-1464`). Findings recorded in `14` §3/§6 and ADR-0018: an ES-module preload does not load under `sandbox: true` and fails silently (the doc's `.mjs` was wrong); `net.fetch` ignores `Range`; the renderer receives no DOM visibility events from window minimise, so `onApplicationPause` needs a lifecycle seam in core; `getWebPreferences()` no longer exists in Electron 44. NOT met until CI runs: installers on Windows and Linux are configuration only (NSIS and AppImage/deb written, never built), the Electron suite has run only on macOS arm64, and nothing is signed or notarised. Follow-ups: encoded storage file names have no length cap (a 512-character key can exceed 255 bytes; both the core and Electron codecs); an application-lifecycle seam so `electron()` can dispatch `onApplicationPause(true)` on minimise; `ThirdPersonCamera` no longer queries physics before the first fixed step (`PhysicsService.hasStepped`, fixed at integration).

### Phase 10 · Devtools (2–3 wk) → 0.9.x

Deliverables (`@ignifx/devtools`): overlay with all panels in `15-devtools-and-diagnostics.md` §4, schema-driven inspector, physics viewer toggle, script hot reload (patch/recreate policies, `onHotReload`), scene reload.

Exit criteria: hot reload tests (state preserved under patch; lifecycle re-run under recreate); inspector edits reflect immediately; zero-cost-when-closed benchmark; devtools skill.

Status (2026-09-06): delivered on `main`. Met locally: `@ignifx/devtools` (138 node tests at 94.9% lines, 7 Chromium tests) ships `app.devtools` with the nine panels of `15-devtools-and-diagnostics.md` §4 — Stats, Scene tree (virtualised), the schema-driven Inspector (every field kind editable with `min`/`max`/`step`/`tooltip`/`readonly`/`hidden`/`group` honoured; `Transform`, which declares no schema, gets a hand-built section), Assets with force reload, Input, Audio, Physics with the Lite debug-viewer toggle, Console, and Timeline — mounted in an `app.ui` layer when `@ignifx/ui` is registered and in its own root otherwise, toggled by a raw backtick `keydown`. **Inspector edits reflect immediately**: the Chromium test edits a `Transform` field and asserts the Lite node moved in the same frame. **Zero cost when closed**: `benchmarks/devtools-closed.test.ts` builds the 1,002-entity scene four times (plain, devtools, devtools, plain) and measures 1,200 frames per variant — 0.1599 ms versus 0.1603 ms median, a +0.0005 ms difference against a 0.02 ms budget on a quiet machine, with zero systems and zero subscriptions registered until `open()` (asserted exactly; the timing bound widens to 15% of the plain median under the parallel `pnpm check`, where a fixed 0.02 ms measured only CPU contention). **Script hot reload** lives in core as `app.hotReload` (`packages/core/src/hot-reload/**`, 36 tests): `"patch"` swaps prototypes and re-files dispatch order, statics and tracked references with no callback re-run; `"recreate"` (`static hotReload = "recreate"`) snapshots props through the schema, destroys and re-creates with the same uid and component index and re-runs `awake`/`onEnable`/`start`; a schema-shape change under `"patch"` falls back to recreate with `IGX-0207`; `static onHotReload(previous)` is class-level; `apply` refuses to run inside a lifecycle callback (`IGX-0208`); `reloadScene(instance)` and the `reloadScenes` watcher re-instantiate a live `SceneInstance` when its file changes. The Vite plugin's `virtual:ignifx/scripts` self-accepts HMR through an explicit `acceptHotReload(app)` export that a production build strips (asserted against a real `vite build`), and a real dev server transforms the module (`dev-server.test.ts`); the plugin now ships `@ignifx/vite-plugin/client` types for both virtual modules, and all four templates register their scripts from the registry with hot reload wired. Exit-criterion tests: state preserved under patch (field value and running coroutine survive, `awake` not re-run); lifecycle re-run under recreate in order with the same uid and restored props. Doc corrections: `15` §4 (the overlay is not unconditionally in an `app.ui` layer; "zero cost" defined; the Console needs a sink) and §5 (no `scripts/index.ts` barrel exists — the registry is the virtual module; the toggle is `app.hotReload.reloadScenes`). Core gaps recorded by the devtools work: no `unregisterSystem` (the sampler outlives a close and returns early), `app.log` had one fixed sink (a sink list was added at integration so the Console panel installs itself), `Assets` publishes no handle listing (the panel walks the manifest), no `world.onHierarchyChanged` (the tree rebuilds on its refresh tick), `Transform` has no schema. NOT verified: a browser HMR round trip that edits a file and observes a patched instance in the page (the generated module, the dev-server transform, the production strip and the headless `apply` are each tested separately); a real changed `.scene.json` driving `reloadScenes`; the physics panel against real Havok in Chromium (stubbed).

### Phase 11 · Docs harness and skill completion (2–3 wk, then continuous)

Deliverables: `docs:api` with TypeDoc markdown for every package; `docs:schemas`; `docs:recipes` from `examples/recipes/*`; example compile/run checks; API-report gate with skill-change heuristic; skill lint; `website/public/llms.txt`; subsystem skills reviewed against one template; `AGENTS.md` finalized (`16-docs-harness-and-skill.md`).

Exit criteria: all harness checks enforced (not advisory) in CI; SKILL.md at most 500 lines; _manual_: an agent given only the repo and the skill builds the "spawn a prefab on click" recipe in a fresh template without reading source (recorded in a tracking issue; replaced by a scripted agent-evaluation harness when tooling permits).

Status (2026-09-06): delivered on `main`. Met locally: every row of `16-docs-harness-and-skill.md` §4 is enforced by `pnpm docs:harness` (and therefore by `pnpm check` and the CI `docs-harness` job): regeneration diff over `docs:api`, `docs:schemas`, `docs:recipes` and the new `docs:llms` (48 generated files); examples compile (65 fenced `ts` blocks across ten skills, 2 justified `ignore-check`); **examples run** — 41 blocks tagged `ts run` execute one Node process each in a throw-away project whose `node_modules` symlinks the workspace packages, 11 s in total, with `file:line` on failure; the API-report gate and freshness against a base ref; skill lint with the frontmatter, link, nesting, deprecated-mention, migrations-pointer and version rules plus the new §2 section-order rule (ten exact `##` headings, subsystems add none), the pre-1.0 Deprecated-section shape, a time-sensitive-phrasing rule, and an exact-import-path rule; `docs:recipes` fails when a recipe has no index row. `website/public/llms.txt` is generated (49 entries, URL mapping in `website/README.md`). The ten skills were reviewed against the template: the electron skill was the one deviation and was reordered; the entry skill is at 490/500 lines. `AGENTS.md` is finalized (status, installing the skill, repository lessons). The **manual exit criterion was run as a scripted agent evaluation**: a fresh agent given only the skill built "spawn a prefab on click" in a scaffolded `3d-third-person` template with zero source reads and zero API-report fallbacks, compiled clean three minutes after it began writing, and proved it headlessly (the spawned crate rests half a box above the floor; the second click's position follows the pointer); its report produced seven documentation fixes (all applied), a third recipe `spawn-a-prefab-on-click`, and two code fixes — `SceneAssetToken` so a script can declare `asset(SceneAssetToken)` for a prefab, and `create-ignifx` no longer copying built `out/`/`release/` output or crashing on symlinks. Findings: `freshness` keyed on any `src/` change false-positived when a change left the public reference identical; it now keys on the package's API report changing. NOT verified: the `ts run` mechanism on Node 24 and on Windows (this machine runs Node 25; symlinks use the `junction` type); the CI job itself has never run.

### Phase 12 · Templates, examples, website content, benchmarks (3 wk)

Deliverables: polish of the four templates (art, audio, menus, settings/rebinding screens, save/load); `examples/` gallery (≥ 12 recipes); `benchmarks/` scenes with baselines (frame time, bundle size per template); website content pages (features, getting started, docs links, gallery) on a prerendered static build with a full visual system (ADR-0019, `website/DESIGN.md`).

Website hosting and design (owner requirement, 2026-09-06): the public site is hosted on **Cloudflare Pages**, connected directly to this repository and built by Cloudflare on every push to `main`. It must therefore build with **one command from the repository root** — `pnpm --filter @ignifx/website build` — into a static output directory, `website/dist`, with Node from `.nvmrc` (24) and pnpm from `packageManager`/`pnpm-lock.yaml`; no server runtime is assumed (Pages Functions only if a later ADR asks for them), URL aliases through `website/public/_redirects`, security headers through `website/public/_headers`, and `website.yml` in CI builds and typechecks the site as a check rather than deploying it. Design and UI/UX quality are a first-class deliverable of this phase, not a follow-up: the site gets a deliberate visual system (typography, colour, spacing, motion), responsive layouts, accessible navigation and contrast, fast first paint, and a reviewed information architecture for the docs, skill, templates and examples it presents.

Exit criteria: bundle ceilings and frame budgets recorded and enforced; the website builds and passes its checks in `website.yml`, and Cloudflare Pages deploys it from `main`; getting-started page verified by following it on a clean machine.

Status (2026-09-07): delivered on `main`. Met locally: all four templates carry a title screen, pause menu, settings screen (bus volumes, render scale, shadows and post-processing toggles), an interactive rebinding page persisted through `app.storage`, an `en`/`fr` language toggle and a versioned save document with checkpoint autosave; audio and art are synthesised deterministically by `pnpm assets`; `benchmarks/baselines.json` records per-template frame-time rows asserted live in the visual suite (medians 0.3/0.3/0.8/0.9 ms against ceilings of 1.0/1.0/2.0/2.0 ms and standards budgets of 2/2/4/4 ms) and as data in `benchmarks/template-frame-time.test.ts`; bundle ceilings re-recorded; `examples/recipes/` holds 16 compiled recipes (49 executed by the harness); the website is a prerendered static export for Cloudflare Pages (ADR-0019, `website/DESIGN.md`) that builds from a fresh clone in one root command, verified on Linux with Node 24, with `website.yml` as its check; the getting-started page was followed on a clean clone and corrected (`pnpm build` before a template's dev server). First CI runs on GitHub (2026-09-06) exposed that no job built before linting, typechecking or testing, and that Linux Chromium loses its WebGPU device at the first canvas present without Vulkan-on-SwiftShader flags; both fixed (ADR-0009). Not met: the Phase 2 target of a core + Lite minimal scene under 250 KB gzipped (hello-cube is 284,142 bytes); Cloudflare Pages itself has not deployed yet (the owner connects it); audio, gamepad and touch were verified structurally, not on hardware. Open follow-ups: a `@ignifx/ui` list/menu/slider widget (each template carries ~1,100 lines of identical menu code), `Dialog` layering undocumented, `PostProcessStack` before-`start()` ordering (fixed in core 2026-09-07), `no-entity-find-in-src` false positives on `actions.find` (resolved 2026-09-07), sprite clip `from`/`to` as index ranges undocumented, the website's `references/api/core` and `ignifx` pages exceed the per-route weight budget by construction.

### Hardening and 1.0 (4–6 wk)

- API review of every package report; rename/removal pass; freeze.
- Two consecutive minor releases (0.9.x → 0.10, 0.11) without breaking changes (`CONSTITUTION.md` §4.3).
- Browser matrix run (Chrome, Edge, Safari 26, Firefox Windows/macOS, Chrome Android, Electron on three OSes).
- Security review of Electron templates and dependency audit; license notices generated.
- Migration infrastructure switched on (`docs/migrations/`, `registerFileMigration`, format version bump path) even though no migration exists yet.
- 1.0 release notes, website launch content _(website launch content delivered on `main` 2026-09-08: the customer-facing ignifx.com with 24 runnable examples and the four templates embedded, the press kit and `/llms.txt` as the one agent entry — `website/plan/08-execution.md` §14 lists what remains for the owner: making the repository public, the Cloudflare settings, browser hardware, and flipping `site.published` on release day)_.

Status (2026-09-07): the items that can be done without publishing are delivered on `main`. API review: `docs/reviews/api-review-2026-09.md` reads all 15 reports (1,439 exports) against the architecture documents — 25 findings applied (release-tag contradictions, duplicates, five renames to the §5.1 rules, eleven internalizations; the umbrella re-exports no `@internal` symbol), 46 left as owner decisions with blast radius, and `audio`, `cli`, `electron` and `vite-plugin` proposed as freeze-ready. Security: `docs/security/electron-review-2026-09.md` (43 items: 25 ok, 10 fixed, 8 accepted risks; two real defects fixed — the navigation lock compared WHATWG origins, which are `"null"` for the `ignifx://` scheme, and the protocol handler served the build from any authority), IPC sender validation (`IGX-1467`), Electron fuses verified on a packaged macOS build, `SECURITY.md`, `pnpm audit` clean on 2026-09-07 (deliberately not a CI gate), `THIRD_PARTY_NOTICES.md` generated by `scripts/licenses.ts` and checked in CI. Engine follow-ups from Phase 12 resolved (post-process stack before `start()`, lint rule, `groupOrRegister`, docs, recipes program, hello-cube 284 → 269 KB); `@ignifx/ui` gained `Menu`/`MenuStack` and the templates dropped 650 duplicated lines each. Not done, and not doable here: two consecutive minor releases (nothing is published; versions are `0.0.0`), the browser matrix beyond Chromium (Safari, Firefox, Chrome Android need hardware and browsers this machine and CI lack; Electron is verified on macOS only, built on three OSes), migration infrastructure (`CONSTITUTION.md` forbids `docs/migrations/` while `0.x`), 1.0 release notes and launch content. Publishing (2026-09-07): the `@ignifx` scope and the `ignifx` name are claimed, all 13 packages hold a hand-published `0.0.0` placeholder, and `release.yml` publishes for real through npm Trusted Publishing — merging the open "Version Packages" pull request is what ships `0.1.0`. `pnpm release:verify` (`scripts/verify-published.ts`) then asks the registry for every version and fails the job if one is missing. Provenance is off in all 13 manifests because npm cannot attest from a private source repository, which is a recorded deviation from `CONSTITUTION.md` §9.4 that making the repository public reverses (ADR-0009). CI: the template frame budgets moved out of `test-visual` into their own `frame-budget` job on `macos-latest`, the machine class their ceilings were recorded on (ADR-0009). Open before a freeze: the 46 review decisions and the 250 KB core target (268,634 bytes).

## 4. Cross-cutting workstreams

| Workstream                  | Ongoing responsibilities                                                                                                                                                                                                                                                                                                |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Upstream (Babylon Lite)** | Track releases weekly; maintain the adapter compatibility suite; file issues/PRs for: public physics step, `onBeforeRender` disposer, collision events with body identities, per-body damping/gravity factor, point-light shadows, device-loss recovery of PCF/CSM and glTF IBL. Upgrade Lite at phase boundaries only. |
| **Performance**             | Benchmarks and baselines from Phase 2 on; budgets in standards §7; profile each template before its release.                                                                                                                                                                                                            |
| **Docs and skill**          | Every PR updates docs; harness gates from Phase 11 become blocking; quarterly "agent onboarding" evaluation.                                                                                                                                                                                                            |
| **Release engineering**     | Changesets discipline; Trusted Publishing; Renovate triage weekly; Electron quarterly bump; TS 7.1 migration when available (remove the TS6 alias, ADR-0007).                                                                                                                                                           |
| **Security**                | Dependency policy, Electron baseline tests, no telemetry audit before each release.                                                                                                                                                                                                                                     |

## 5. Risk register

| Id   | Risk                                                         | Likelihood / impact                 | Mitigation                                                                                                                                                        |
| ---- | ------------------------------------------------------------ | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R-1  | Lite breaking changes every few releases                     | High / Medium                       | Exact pin, adapter boundary (ADR-0002), compatibility suite, upgrade only at phase boundaries                                                                     |
| R-2  | TS 7 tooling gap (no API until 7.1) breaks a tool we rely on | Medium / Medium                     | TS6 alias recipe (ADR-0007); tsdown dts path needs no API; Oxlint primary                                                                                         |
| R-3  | Lite docs and `index.d.ts` disagree                          | High / Low                          | Treat `.d.ts` as truth; adapter tests per API used; inventory refreshed at each upgrade                                                                           |
| R-4  | Collision events lack body identities                        | Certain today / High                | Upstream PR first; ADR-0013 opt-in internals drain with layout guard as fallback; trigger events documented as the gameplay default until resolved (Phase 4 S4.2) |
| R-5  | Runtime-added meshes appear late (material swap compile)     | Medium / Medium                     | Warm-up of material families before `registerScene`; `renderer.warmUp`; document                                                                                  |
| R-6  | Mesh removal is terminal in Lite                             | Certain / Medium                    | Visibility toggles for hide; destroy recreates; tests                                                                                                             |
| R-7  | WebGPU unavailable on Firefox Linux/Android, older devices   | Certain / Medium                    | Clear unsupported page; matrix documented; no fallback by decision (ADR-0001)                                                                                     |
| R-8  | Electron WebGPU flag/driver issues, especially Linux         | Medium / Medium                     | S9.1 spike on three OSes; flags in the window factory; Linux best-effort                                                                                          |
| R-9  | Rapier 2D bundle/determinism issues                          | Low / Medium                        | S6.2 spike; Box2D v3 fallback (ADR-0006)                                                                                                                          |
| R-10 | Performance of sprite sync and tilemaps in Lite layers       | Medium / High for 2D                | S6.1 spike early in Phase 6; chunking, static flags, SoA buffers                                                                                                  |
| R-11 | Agents drift docs or introduce undocumented API              | Medium / Medium                     | Harness gates, API report gate, review rules (standards §15)                                                                                                      |
| R-12 | Scope creep toward an editor                                 | Medium / High                       | Constitution §1.3; editor explicitly post-1.0; schemas/inspector metadata keep the door open                                                                      |
| R-13 | WASM payloads (Havok ~2 MB, Recast, Rapier) hurt first load  | Medium / Medium                     | Lazy loading options, preload groups, manifest-served `.wasm` with caching                                                                                        |
| R-14 | Determinism differences across CPUs/WASM engines             | Medium / High for future networking | Three-OS determinism gate from Phase 4; published list of guaranteed vs best-effort operations; per-platform determinism is the only promise (`09-physics.md` §8) |

## 6. Dependency graph

| Phase                                     | Requires                                                                           |
| ----------------------------------------- | ---------------------------------------------------------------------------------- |
| P1 Kernel                                 | P0                                                                                 |
| P2 Rendering and assets                   | P1                                                                                 |
| P3 Input                                  | P2                                                                                 |
| P4 Physics 3D                             | P2                                                                                 |
| P5 Audio                                  | P2                                                                                 |
| P6 2D toolkit + 2D physics                | P3, P4 (shared stepping model and interpolation)                                   |
| P7 3D toolkit                             | P3, P4                                                                             |
| P8 UI                                     | P3                                                                                 |
| P9 Platform                               | P2 for storage and `@ignifx/electron`; P6 and P7 for the desktop template variants |
| P10 Devtools                              | P8 (overlay host), P1 (schemas)                                                    |
| P11 Harness completion                    | P10; harness checks accumulate from P0 onward                                      |
| P12 Templates, examples, site, benchmarks | P6, P7, P8, P9, P10                                                                |
| Hardening → 1.0                           | P11, P12                                                                           |

## 7. MVP checklist (what "feature complete" means)

Ticked 2026-09-07 against the phase status lines above; "feature complete" here means delivered on `main` and gated, not released.

- [x] `createApp`, extensions, settings, headless mode — Phase 1
- [x] Entity/Component/Script lifecycle exactly as `01-lifecycle-and-time.md` — Phase 1
- [x] Scene format v1 with instances/overrides; save/load round trip — Phase 2; template saves in Phase 12
- [x] Assets with refcounts, progress, cancellation, manifest, hot reload — Phases 2 and 10
- [x] Rendering: cameras (persp/ortho), lights + shadows, meshes, glTF models, materials, environment, post stack (bloom/SMAA/tone), picking, device loss — Phase 2; post stack before `start()` fixed in hardening
- [x] Input action maps across keyboard/mouse/gamepad/touch, pointer lock, rebinding — Phase 3; rebinding screens in Phase 12
- [x] Physics 3D with interpolation, triggers/collisions, character controller, queries — Phase 4
- [x] Audio buses, spatial sources, music — Phase 5
- [x] 2D: camera, sprites, animation, atlases, tilemaps, parallax, Y-sort, pixel-perfect, 2D physics — Phase 6
- [x] 3D: character and camera rigs, Animator, navigation — Phase 7
- [x] UI overlay host, anchors, text, virtual controls, i18n — Phase 8; `Menu`/`MenuStack` in hardening
- [x] Storage; Electron package and desktop templates — Phase 9; security review in hardening
- [x] Devtools overlay and hot reload — Phase 10
- [x] Docs harness enforced; entry + subsystem skills; llms.txt; website shell with content — Phases 11 and 12
- [x] Four templates, examples gallery, benchmarks with baselines — Phase 12

## 8. Post-1.0 roadmap (not committed)

Editor (schema-driven inspector → scene editor), multi-camera/render targets, worker rendering (`OffscreenCanvas`), particles (Lite node particles), 2D lighting, networking (lockstep/server-authoritative on the deterministic loop), XR when Lite's path is runnable, asset packs, audio effects, root motion, 2D blend spaces, save-game migrations tooling.

## 9. Phase 0 kick-off checklist

1. Claim the `@ignifx` npm scope and the `ignifx` package name (both unclaimed on 2026-09-05); set up npm Trusted Publishing for the GitHub repository.
2. Create the workspace per `docs/standards/coding-standards.md` §2–§3 with the catalog pins.
3. Run spikes S0.1 and S0.2; record results in `docs/adr/0009-monorepo-tooling.md` (append "Validation").
4. Land CI with all jobs; make `main` protected (required checks, one human approval).
5. Publish `0.0.1` dry run; verify `npx skills add` finds `skills/ignifx/SKILL.md`.
