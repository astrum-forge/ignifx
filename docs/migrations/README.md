# Migrations

Intentionally empty. Per `CONSTITUTION.md` §4.2 no migration documents are written before 1.0; breaking changes are listed under **Breaking** in the changelog. After 1.0 (§4.4) every breaking change ships with `docs/migrations/<from>-to-<to>.md`. CI fails if any other file appears here while the version is `0.x`.
