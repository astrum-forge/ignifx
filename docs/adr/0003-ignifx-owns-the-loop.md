# ADR-0003 · ignifx owns the frame loop: fixed timestep, simulation scene, engine-driven animation

**Status:** Accepted · **Date:** 2026-09-05

## Context

Verified against `@babylonjs/lite@1.27.0`: Lite's `createHavokWorld` inserts a step callback at the front of `scene._beforeRender`, runs exactly one Havok step per rendered frame (no accumulator), and cannot be unregistered through public API; glTF animation groups added to a scene are advanced by a hook that runs after user callbacks; `onBeforeRender` callbacks run in reverse registration order; `stepScene(engine, scene, deltaMs)` runs a scene's update on a null engine. `CONSTITUTION.md` §2.1 and §3.2 require deterministic fixed-step simulation and a single loop definition.

## Options considered

1. **Let Lite step physics once per frame** — simplest; matches Babylon.js defaults. Cons: not fixed-step under variable frame rates; no interpolation; `lateUpdate` before animation.
2. **Drive Lite's render scene `_update` multiple times per frame** — uses private API; re-runs animation hooks per step.
3. **Host physics on a separate headless simulation scene stepped by ignifx; detach animation groups from Lite and advance them with `updateAnimationManager`** — public API only; full control of ordering and step count.

## Decision

Option 3. One ignifx callback per world runs the whole frame (`docs/architecture/01-lifecycle-and-time.md` §3). Physics lives on a null-engine `SceneContext` and is stepped with `stepScene` inside ignifx's accumulator loop; interpolation is implemented by ignifx. Animation groups are removed from asset containers before `addToScene` and advanced in `PostUpdate` by ignifx-owned `AnimationManager`s. Sprite animation likewise runs on ignifx's clock.

## Consequences

- Determinism and Unity-order semantics (`update → animation → lateUpdate`) are guaranteed regardless of Lite's internal ordering.
- Two Lite scenes per world (render + simulation); bodies bind to the same `SceneNode`s.
- Upstream requests: public `stepPhysicsWorld`, `onBeforeRender` disposer, collision events with body identities. If Lite adds them, the adapter simplifies with no public change.
- Risk: a future Lite version could couple physics to the render scene; the adapter compatibility suite will catch it at upgrade time.

## Validation (2026-09-05)

Spikes S1.1, S1.2 and S1.3 were run against `@babylonjs/lite@1.27.0` and `@babylonjs/havok@1.3.14` on macOS 26 (Darwin 25.5, arm64), Node 25.2.1, and Playwright Chromium with `--enable-unsafe-webgpu --use-webgpu-adapter=swiftshader`. The adapter modules they produced are `packages/core/src/lite/{node,loop,scene}.ts`; the tests are `packages/core/test/lite/**`. Line numbers are `@babylonjs/lite/index.d.ts` unless a `lib/` path is given.

### S1.1 — Transform on Lite nodes

| Fact                                                                                                                                                                                                                                                                                                                                             | Evidence                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `createTransformNode(name, …)` is `createSceneNode` with default TRS; a node is a plain object with three observable values, a cached matrix and closures                                                                                                                                                                                        | 3385, `lib/scene/transform-node.js:5`, `lib/scene/scene-node.js:53`                                                                                       |
| `TransformNode` is an alias of `SceneNode`; there is no distinct type                                                                                                                                                                                                                                                                            | 13093, 10104                                                                                                                                              |
| A pure transform node never needs `addToScene` — `worldMatrix` is correct the moment the node exists                                                                                                                                                                                                                                             | nothing in `lib/scene/world-matrix-state.js` reads the scene; test "computes a valid world matrix without ever adding the node to a scene"                |
| `worldMatrix` is a getter. On a cache miss it composes local TRS and returns `parentWorld × local` (column-major, translation in 12/13/14); with no parent it returns the local matrix object itself                                                                                                                                             | `lib/scene/world-matrix-state.js:68-84`, `lib/math/mat4-multiply-into.js`                                                                                 |
| Invalidation is **pushed** down, not polled: a node's world-matrix state keeps the states of the children linked through `parent` and calls `_invalidate()` on all of them                                                                                                                                                                       | `lib/scene/world-matrix-state.js:26-33, 46-62`                                                                                                            |
| Exception: a parent that is a bare `IWorldMatrixProvider` (a camera) cannot be pushed to, so the child polls `parent.worldMatrixVersion` on every `worldMatrix`/`worldMatrixVersion` read                                                                                                                                                        | `lib/scene/world-matrix-state.js:34-40, 69-71, 86-89`; 6537                                                                                               |
| `worldMatrixVersion` bumps on every TRS invalidation. Per-component setters (`position.x = v`) compare first and stay silent when the value is unchanged; the bulk `set(...)`/`copyFrom(...)` notify **unconditionally**                                                                                                                         | `lib/math/observable-vec3.js:12-40`; tests "bumps worldMatrixVersion when a TRS value changes…" and "bumps a bulk set even when the values are unchanged" |
| `Mat4` is a `Float32Array` today, but Lite documents it as opaque ("callers MUST NOT depend on the underlying storage"), so the adapter copies element by element                                                                                                                                                                                | 6981; `lib/math/_matrix-allocator.js:3-4` (`F32 = Float32Array`)                                                                                          |
| Hand-computed check: parent `T(1,2,3)·R(+90° Y)·S(2)` with child `T(1,0,0)` puts the child's world origin at exactly `(1, 2, 1)`; a five-level chain matches the product of the locals computed with plain arithmetic                                                                                                                            | `test/lite/node.test.ts`, expectations derived in `test/lite/fixtures/matrix.ts`                                                                          |
| `setParent` preserves the world transform and maintains **both** `children` arrays itself. Under a singular parent matrix it keeps the link but copies only the world _position_ into the local position; the local TRS it writes comes from `mat4Decompose`, which is canonical rather than sign-faithful (a mirror becomes a negative Y scale) | 10603/10605 and its TSDoc, `lib/scene/set-parent.js`, `lib/math/mat4-decompose.js`                                                                        |
| Lite has no "keep local values" reparent, so `linkParent` assigns `node.parent` (the setter does the state re-registration and invalidation) and maintains the `children` arrays itself                                                                                                                                                          | `lib/scene/world-matrix-state.js:46-62`; test "keeps local values when linkParent moves a node"                                                           |
| `setSubtreeVisible` materializes the cascade at write time by walking `children`; a node linked afterwards does **not** inherit the state                                                                                                                                                                                                        | 11199, `lib/scene/visibility.js`; test "cascades visibility over the subtree at write time"                                                               |
| `metadata` is an open record (`{ gltf?: GltfMetadata; [key: string]: unknown }`), so `metadata.ignifx` coexists with glTF extras                                                                                                                                                                                                                 | 6688                                                                                                                                                      |
| `mat4Decompose` returns `{ translation, rotation, scale }` as three freshly allocated objects, so it is not yet a zero-allocation path even with `out` parameters                                                                                                                                                                                | 3717, 7013, `lib/math/mat4-decompose.js`                                                                                                                  |
| Releasing a pure transform node needs nothing from Lite — no GPU resource, no ref count, no scene registry — but the parent link must be broken, because a parent retains its children's world-matrix states                                                                                                                                     | `lib/scene/scene-node.js`, `lib/resource/ref-count.js` (meshes only); `disposeNode` in `src/lite/node.ts`                                                 |

### S1.2 — One callback per world, and where Havok sits

| Fact                                                                                                                                                                                                                                                                                                                                                                                  | Evidence                                                                                                                      |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| `onBeforeRender(scene, cb)` is `scene._beforeRender.unshift(cb)`, and `_update()` walks the list front to back — **later registrations run earlier**                                                                                                                                                                                                                                  | 7722, `lib/scene/scene-core.js:83`, `:50`                                                                                     |
| `createHavokWorld` unshifts its step callback the same way                                                                                                                                                                                                                                                                                                                            | 2592, `lib/physics/havok.js:60`                                                                                               |
| **Ordering finding.** Registration order alone decides. An ignifx callback registered _after_ `createHavokWorld` runs **before** Havok's step and therefore sees the previous step's poses; registered _before_ it, it runs after the step. "Inserted at the front of the list" means _first to be registered ends up last to run_, which is the opposite of what the phrase suggests | `test/lite/havok-order.test.ts`, both directions asserted on a falling body                                                   |
| Havok performs exactly one `HP_World_Step` per scene step, using `fixedDeltaMs` when it is set and the step's `deltaMs` otherwise, capped at 100 ms, with no accumulator                                                                                                                                                                                                              | `lib/physics/havok.js:71-96`; test "steps physics exactly once per scene step at the delta it was given"                      |
| Havok's callback _can_ be removed — `createHavokWorld` stores a `_stopStep` closure on the world — but only through a private field, so ADR-0003's "cannot be unregistered through public API" stands                                                                                                                                                                                 | `lib/physics/havok.js:61-67`                                                                                                  |
| There is no public unregister for `onBeforeRender`. The adapter therefore keeps the Lite-held closure forever and guards it with a handle flag (`registerFrameCallback`/`unregisterFrameCallback`)                                                                                                                                                                                    | `lib/scene/scene-core.js` exports no remover; `test/lite/loop.test.ts`                                                        |
| `stepScene(engine, scene, deltaMs)` is exactly `engine._currentDelta = deltaMs; scene._update()`, and `_update` hands callbacks `scene.fixedDeltaMs > 0 ? scene.fixedDeltaMs : engine._currentDelta`. With `fixedDeltaMs` at its default `0`, `runFrame(engine, scene, 1/60)` delivers **exactly** `1000/60` — `(1/60) * 1000` and `1000/60` are the same double                      | 12438, `lib/engine/null-engine.js:22-25`, `lib/scene/scene-core.js:44`; test "delivers exactly the delta the step was given…" |
| Trap: setting `scene.fixedDeltaMs` makes Lite ignore the step delta entirely, so ignifx must never touch it                                                                                                                                                                                                                                                                           | test "ignores the step delta once scene.fixedDeltaMs is set"                                                                  |
| `runHeadlessSteps(engine, scene, steps, deltaMs = 1000/60)` is a plain loop over `stepScene`; the adapter drives its own loop instead so ignifx owns the accumulator                                                                                                                                                                                                                  | 10002, `lib/engine/null-engine.js:26-30`                                                                                      |
| **Havok under Node.** `@babylonjs/havok@1.3.14`'s ESM build hard-codes `ENVIRONMENT_IS_WEB = true` and fetches the `.wasm` with `fetch(scriptDirectory + path)`, which Node refuses for `file:` URLs, so `locateFile` cannot help. Passing Emscripten's `wasmBinary` with bytes read through `node:fs` works, and the full Havok world runs headlessly in Node                        | `lib/esm/HavokPhysics_es.js`; `test/lite/havok-order.test.ts` `loadHavok()`                                                   |
| Phase 4 consequence: the simulation scene can be a null-engine `SceneContext` in Node as well as in the browser, so physics tests need no GPU                                                                                                                                                                                                                                         | four Havok tests pass in the Vitest `node` project                                                                            |
| `startEngine`/`stopEngine` and `registerScene` behave as documented; `startEngine` resolves after the first rendered frame, so a browser test needs no wall-clock sleep                                                                                                                                                                                                               | 9535, 12319, 12462; `test/lite/loop.browser.test.ts`                                                                          |

### S1.3 — Determinism

| Fact                                                                                                                                                                                                                                                                                                                    | Evidence                                                       |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| 50 nodes (a 10-deep chain plus 40 siblings), one ignifx frame callback, 600 steps of `1000/60` ms: two independent runs in one process produce the same hash, and two interleaved runs produce the same hash as a run on its own                                                                                        | `test/lite/determinism.test.ts`                                |
| The same scene, the same 600 steps, driven on a **real WebGPU engine's** render scene in Chromium reproduces the identical hash                                                                                                                                                                                         | `test/lite/determinism.browser.test.ts`                        |
| `stepScene` is engine-agnostic in 1.27.0 — it only writes `engine._currentDelta` and calls `scene._update()` — so a render scene with Lite's default render task can be advanced without starting the rAF loop and without recording GPU work                                                                           | `lib/engine/null-engine.js:22-25`; the browser test drives one |
| Baseline: `EXPECTED_HASH = "7b79fbda"`, FNV-1a over the big-endian `Float64` bit patterns of all 16 elements of all 50 world matrices, read through the adapter's `readWorldMatrix`                                                                                                                                     | `test/lite/fixtures/determinism-expected.ts`                   |
| The animation uses only integer arithmetic and additions of multiples of `2^-12`, so it introduces no rounding of its own; `Math.sin`/`Math.cos`/`Math.sqrt` and division are avoided because the ECMAScript specification lets implementations approximate them, which would make a Node-vs-Chromium comparison unfair | `test/lite/fixtures/determinism-scene.ts`                      |

## Validation — Phase 4 (2026-09-06)

Spikes S4.1 and S4.3 were run against `@babylonjs/lite@1.27.0` and `@babylonjs/havok@1.3.14` on
macOS 26 (Darwin 25.5, arm64), Node 24.20.0, and Playwright Chromium with `--enable-unsafe-webgpu
--use-webgpu-adapter=swiftshader`. The adapter modules they produced are
`packages/physics/src/lite/**`; the tests are `packages/physics/test/lite/**` and
`packages/physics/test/physics/**`. Line numbers are `@babylonjs/lite/index.d.ts` unless a `lib/`
path is given. S4.2 is recorded in ADR-0013 instead, because its outcome activates that waiver.

### S4.1 — the simulation scene

| Fact                                                                                                                                                                                                                                                    | Evidence                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `createHavokWorld(scene, hknp, gravity)` works on a `createNullEngine()` scene with `defaultRenderTask: false`, and `stepScene(engine, scene, deltaMs)` performs exactly one `HP_World_Step`                                                            | 2592, 12438, `lib/physics/havok.js:44,73`; `test/lite/simulation-scene.test.ts`                                                                       |
| `setPhysicsTimestep(world, dt)` stores `dt * 1000` in `world._fixedDeltaMs`, and `getPhysicsTimestep` reads it back exactly, so Lite's per-step delta and ignifx's fixed step never disagree                                                            | 10863, 5921, `lib/physics/havok.js:151`                                                                                                               |
| The simulation scene holds no meshes and no lights, as `09-physics.md` §1 requires                                                                                                                                                                      | `test/lite/simulation-scene.test.ts` asserts both counts are `0`                                                                                      |
| Bodies bind to plain `SceneNode`s, so the entity's `transform.lite` node _is_ the body's node and reading `transform.position` reads the simulation                                                                                                     | `test/physics/free-fall.test.ts`                                                                                                                      |
| **`_syncBodyToNode` writes the node's LOCAL TRS** (`node.position`, `node.rotationQuaternion`), so a body's entity must be a root entity for its pose to be world space                                                                                 | `lib/physics/havok.js:112-120`; the extension reports `IGX-0907` otherwise                                                                            |
| Two worlds stepped 600 times each produce bit-identical poses, and a whole ignifx app reproduces a committed hash across two apps, interleaved runs, and three consecutive processes                                                                    | `test/lite/simulation-scene.test.ts`, `test/determinism/**` (`EXPECTED_PHYSICS_HASH = "2301e7d4"`)                                                    |
| **Havok's first step under-integrates gravity.** Every step after the first adds exactly `g·h²` to the drop, but the first adds about `0.61·g·h²`, so a free fall lands between the exact parabola and the semi-implicit sum                            | `test/physics/free-fall.test.ts`, which asserts the analytic value to within half a step of velocity (`½·g·h·t`) and the per-step increment to `g·h²` |
| Under Node, Havok is instantiated from Emscripten's `wasmBinary`; the extension reads the binary out of the installed `@babylonjs/havok` package when no server answers the manifest address                                                            | `src/lite/havok-module.ts`, `test/lite/havok-module.test.ts`                                                                                          |
| In the browser the binary is served through the asset manifest at the **bare file name** `HavokPhysics.wasm`, because the Vite plugin copies extension public assets unhashed and by base name into the same directory the default asset root points at | `packages/vite-plugin/src/plugin.ts` `generateBundle`; `test/lite/browser.browser.test.ts`                                                            |

### S4.3 — interpolation never leaks into Havok

| Fact                                                                                                                                                                                                      | Evidence                                                                       |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| `_stepWorld` pre-syncs node→body only for `ANIMATED` bodies and for bodies with `_preStep` set, so a `DYNAMIC` body never reads its node and a display pose written between steps cannot reach the solver | `lib/physics/havok.js:85-94`                                                   |
| A dynamic body interpolated over 40 irregular frames produces byte-for-byte the same authoritative poses as the same body with `interpolation: "none"`                                                    | `test/physics/interpolation.test.ts`                                           |
| A kinematic body — which _is_ pre-synced — needs the `FixedUpdate -100` restore, and with it produces the same poses as an uninterpolated one                                                             | same file, "restores a kinematic body's authoritative pose before fixedUpdate" |
| The display pose really is between the two snapshots: at `fixedStepAlpha > 0` the node differs from the pose `fixedUpdate` sees                                                                           | same file                                                                      |
